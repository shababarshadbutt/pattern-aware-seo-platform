{
  // Typecheck config: covers src and the integration tests in test/.
  // rootDir/outDir live in tsconfig.build.json.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*", "test/**/*", "vitest.config.ts"]
}
