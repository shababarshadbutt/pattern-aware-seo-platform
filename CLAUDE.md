Pattern-Aware SEO Platform — Agent Guide

This file is read automatically by Claude Code (and should be treated as load-bearing by any other coding agent working in this repo). It tells you what this project is, what's already been decided, and what rules to follow so the codebase stays consistent instead of accumulating whatever style each session or contributor happens to reach for.

Read before making non-trivial changes
docs/architecture-review-and-action-plan.md — the why: the product direction, the phased build order (Phase 0 through 5), and a list of open decisions that haven't been made yet. Don't build ahead of the current phase without checking this. As of M6, this doc carries dated amendment sections through M0–M6, the post-M3 hardening pass, and both main promotions (PR #7, then the bundled M4+M5 promotion PR #9) — read to the end, not just the original text.
docs/CODING_STANDARDS.md — the how: full TypeScript/Node.js and PostgreSQL conventions. This file only summarizes the parts an agent is most likely to violate by default. As of M6 this includes a full §1.13 on composition correctness — the single most common real-bug category in this codebase (five confirmed instances across M2, M5, and M6: independently-correct, independently-tested pieces that break an invariant neither one owns once wired together) — plus recorded exceptions/additions on composite primary keys on partitioned tables, the Biome + narrow type-aware ESLint pass, a documented departure from "port the legacy algorithm" when the legacy algorithm is provably wrong, never using a sampling hash as a uniqueness key, capability-scoped config validation, and end-to-end-against-real-dependencies as a required check alongside per-package tests — check there before assuming the general rule applies unmodified.
docs/DESIGN.md — the UI spec: visual identity, tokens, component rules. Binding for anything touching apps/web. Rewritten at D1 (2026-09-04): it now describes a Google Stitch design that SUPERSEDED the previous "glass cockpit instrument panel" identity — indigo accent, Geist + JetBrains Mono, dark only, 2/4/8/12 radii. Do not reason from a memory of the amber/instrument-panel spec; see ADR-0027 for what changed and, more importantly, for the four things a redesign did NOT get to change.
docs/decisions.md — ADR log. Check here before re-litigating something already decided during implementation (tooling choices, version pins, schema exceptions, algorithm replacements, etc.) — as of M2 this includes at least ADR-0008, ADR-0010, and ADR-0012.

If a task touches something listed under "Open decisions" in the action plan (e.g. whether PopulationProfile duplicates existing tables, or the internal-vs-external auth question), stop and surface the decision instead of guessing.

Document precedence when two docs conflict

These docs were written at different times for different purposes, so a conflict between them is expected occasionally, not a sign something's broken. Resolve it in this order:

docs/decisions.md (ADRs) — the most recent, most specific, ground-truth record of what was actually implemented and why.
docs/DESIGN.md — wins over the architecture/tech-stack doc for anything about UI presentation, motion, or the frontend design-tooling recommendation. It's the more specific, later document for that surface. (Confirmed case, as of D1: the architecture doc's design-tooling section recommends Impeccable plus the shadcn/ui MCP and describes Magic UI motion moments; docs/DESIGN.md now sources the design from Stitch, enforces it through the token-namespace reset instead of a taste audit, and bans decorative animation outright. DESIGN.md wins — that's not a contradiction to "fix," it's the general recommendation being overridden by a more specific, later one. The architecture doc carries a dated amendment saying so rather than being rewritten.)
docs/architecture-review-and-action-plan.md — the overall product/data-model/infra direction.
docs/CODING_STANDARDS.md — general code conventions; expect this one to accumulate documented exceptions as real tooling, schema, and algorithm decisions get made (e.g., a narrow type-aware lint rule sourced from a second tool alongside the primary linter/formatter, a composite primary key on partitioned tables, or a legacy algorithm replaced after a corpus test proved it wrong) rather than being rewritten each time.

When a conflict surfaces, add an ADR recording which side won and why — don't just silently pick one.

What this is

A pattern-aware, sampling-based SEO intelligence platform. It does not crawl every URL on a site. It collapses a sitemap's URLs into a much smaller set of patterns, samples each pattern statistically (with proper confidence intervals, not guesses), and verifies health with targeted HTTP checks — the entire point is auditing a 90M-URL site without making 90M requests. Any change that reintroduces "just crawl everything" thinking is working against the product's actual differentiator.

The core entity model (landed in M1) is organization → site → sitemap_run → sitemap_file → pattern → pattern_population / pattern_sample → sample_observation, plus audit_snapshot and sampling_health. site exists independently of any one audit run — this is the deliberate fix for the legacy tool's design, where the root entity was a one-off session with no memory of a site being audited again later. Trends, regressions, a fleet view across sites, and history to train a model on all depend on site being a durable entity, not an artifact of a single run. Don't reintroduce a run-rooted model even locally in new code.

As of M2, single-pass streaming ingestion is real and benchmarked (10,000,000 URLs, 681 MB peak RSS against a 1,536 MB budget, ~41,600 URLs/s, byte-identical resume). Pattern extraction does not faithfully port the legacy algorithm — the legacy algorithm had a real correctness defect (see ADR-0012, and the Non-negotiable rules below) and was replaced with a prefix trie after corpus testing proved the defect out. Don't assume "ported from legacy" describes packages/sitemap's pattern extraction; check docs/decisions.md first.

Repo layout
apps/
  web/       Next.js frontend (dashboard)
  api/       Fastify API
  worker/    BullMQ workers + Piscina pools
packages/
  database/   Schema shared by api + worker. Tenant-scoped queries are enforced at compile time via an
              opaque `Database` type with no ambient query methods and a single package entry point (`.`)
              — see `compile-guards.test.ts` and ADR references in `docs/decisions.md`. Don't add a
              second way to obtain a raw client that bypasses this. A narrow, test-only
              `packages/database/testing` export subpath (a migrated-pool factory for packages like
              `packages/pipeline` that need a real Postgres in their own suite) is the one sanctioned
              exception — decided at M6, implemented at M7 with both of its conditions met
              (ADR-0023): one compile guard asserts the exact sanctioned export set, and a second
              asserts the subpath is imported only from test files and never from `apps/*/src`, which
              is why `apps/api`'s tests live in `apps/api/test/` rather than beside the source. The
              `.` entry point is unchanged and no less restrictive — the harness hands back the same
              opaque `Database`, so a test can create and drop a database and still cannot write an
              unscoped query. `sample_observation` is unique on the URL, not on `url_hash` —
              see the Non-negotiable rules below before adding any new uniqueness constraint on a hash
              column. Isolation is enforced at two independent layers — compile-time (the opaque type)
              and schema-time (`UNIQUE (site_id, id)` plus FK pairing, closed post-M3) — each needs its
              own proof; don't treat one as covering the other.
  sampling/   Bounded min-heap-by-hash sampling (landed M2) plus Wilson score confidence intervals with
              finite-population correction and adaptive sample expansion (landed M3, validated against
              published reference values, not just the package's own arithmetic).
  sitemap/    Streaming SAX parser + pattern extraction (prefix trie — a deliberate, ADR-0012-recorded
              departure from the legacy algorithm, not a port of it). Maintains a large adversarial
              synthetic corpus test (10M-URL scale) alongside unit tests — see
              `docs/CODING_STANDARDS.md` §1.10 before touching this package with only small fixtures
              as evidence of correctness.
  verification/ HTTP verification client (landed M5; the directory is `verification/`, NOT `http/`): HEAD-first, GET-only-on-suspicious, size-capped GET
              bodies, per-request rate limiting, circuit breaking. Its own test suite is deliberately
              entirely mocked at the transport boundary (see `docs/CODING_STANDARDS.md` §1.10 on why) —
              real-socket lifecycle bugs (e.g. the M6 `probe.ts` teardown defect below) only surface
              when this package is exercised end-to-end against a real server, not from its own suite.
  pipeline/   Orchestrates discover → ingest → verify → estimate → finalize (landed M6) behind per-site,
              per-tier queues (`{tier}:{siteId}:{stage}`). BullMQ appears nowhere inside this package —
              every stage takes its dependencies as injected parameters, so the package's own test suite
              runs against a real Postgres and a real local HTTP server with zero Redis involved; queueing
              is composed in from outside the package (see `apps/worker`).
  shared/     Shared types, utils, constants (zod-validated startup config, logger, domain error hierarchy).
              Config validation is capability-scoped (separate `dbConfig`/`redisConfig`/`httpConfig`
              sub-schemas an entry point composes from what it actually consumes), not one monolithic
              `getConfig()` — see `docs/CODING_STANDARDS.md` §1.7 and the M6 `runIngest` finding below.
  ml-client/  Client for the ml-service prediction endpoint
ml-service/   Separate Python service (model training + /predict) — see rule below
infrastructure/  Terraform/CDK for AWS
docs/         Architecture plan, coding standards, design spec, ADR log
Commands
bash
docker compose up -d        # Postgres 16 + Redis (ports overridable via POSTGRES_PORT/REDIS_PORT —
                             # see .env if a native Postgres/Redis install on the host already holds
                             # the default port)
pnpm install
pnpm dev                    # runs web + api + worker together (from repo root)
pnpm --filter <app> dev     # run just one app, e.g. pnpm --filter worker dev
pnpm build
pnpm lint
pnpm typecheck
pnpm test

Run pnpm dev from the repo root, not from inside an app folder — a package's own dev script only exists once that package has real source files.

Non-negotiable project rules

These come out of a specific architecture review and its critique (see the action plan doc for the full reasoning) — they are not arbitrary preferences, and deviating from them reintroduces problems that were already identified and fixed once.

No ORDER BY random() for sampling, ever, on tables that can grow large. Use the bounded min-heap-by-hash approach in packages/sampling — implemented and benchmarked at 10M-URL scale as of M2.
Confidence math must use the Wilson score interval with finite-population correction, not a naive normal approximation. This lives in packages/sampling — don't recompute it ad hoc elsewhere. (Check docs/architecture-review-and-action-plan.md's Phase 0 amendment and docs/decisions.md for the corrected FPC details verified during M0 — the original doc's threshold language here was found to need correction.) The degenerate-interval defect (a [0,0] interval falsely implying certainty from a partial sample) is now a database-level CHECK constraint, not just an application check, with an n = N exemption for a fully-counted population — see M1 notes below. The Wilson/FPC math landed in M3 (validated against published reference values, not just the package's own arithmetic); what M2 delivered before it was the sampling mechanism the math consumes — the min-heap — not the math.
HTTP verification is HEAD-first, GET only on suspicious results, and GET body fetches must be size-capped (soft-404 detection needs the first few KB, not the whole page). Cap how much of a pattern's sample is allowed to escalate to GET before flagging it for manual review instead of continuing. (Same note as above — the body-cap specifics were corrected during M0 verification; defer to docs/decisions.md.)
Never materialize a full URL population in memory or in a single query result. Everything works off streaming passes and pattern/file-level aggregates, not per-URL row sets at the 10M+ scale. M2's benchmark (681 MB peak RSS for 10M URLs) is direct evidence this is holding in practice, not just in intent.
A run that completes without throwing is not necessarily a run that succeeded. A sitemap parse that yields zero URLs from an accepted file is indistinguishable from a legitimately empty site unless it's flagged explicitly (an HTML error page parses as valid XML — this bit M2). Any code path that can "succeed" with an empty or suspicious result must signal that explicitly (structured warn log at minimum) rather than looking identical to an ordinary success. See docs/CODING_STANDARDS.md §1.5.
Multi-tenant isolation is a first-class concern, not an afterthought. Large per-URL/per-pattern tables are partitioned by site_id from schema creation (as of M1, six tables), not retrofitted later. Unscoped, cross-tenant queries against packages/database are a compile error, not a runtime risk, by construction (opaque Database type + single entry point) — see compile-guards.test.ts — and a schema-level cross-tenant FK gap found post-M3 is closed (UNIQUE (site_id, id) plus five repointed FKs). BullMQ queues and Redis keys are namespaced per site/tier ({tier}:{siteId}:{stage}) — done as of M6, closing the item pending since M2. Connection-pool budgeting is the one piece still not built. Fleet-wide auto-attach (a worker automatically picking up newly-added sites across organizations) needs a cross-organization read, which would punch a hole in this guarantee — deliberately left undecided as of M6; don't wire it in incidentally while touching queues or workers.
Query layer split: use Drizzle or Kysely (not Prisma) for anything in packages/sampling or packages/sitemap — this workload needs hash filtering, window functions, and CTEs close to raw SQL. Prisma is fine for simple CRUD (dashboard/admin, user/site management) if used there, but never for the sampling hot path. Drizzle Kit is the schema migration source of truth, but it cannot generate PARTITION BY DDL — partitioned-table migrations are hand-completed with three verification guards (banner comment, a relkind = 'p' test against a real database, a test asserting a PARTITIONED_TABLES constant matches what the database reports). Never run drizzle-kit push against this schema — see docs/CODING_STANDARDS.md §2.4.
Never make a uniqueness constraint out of a hash chosen for its sampling distribution properties. sample_observation was originally unique on the 32-bit FNV-1a url_hash, which — combined with the min-heap sampler specifically retaining the smallest hashes — clustered collisions in exactly the retained sample and silently dropped real observations. Fixed in M2 (migration 0001) by moving uniqueness to the URL. See docs/CODING_STANDARDS.md §2.2 before adding any new uniqueness constraint on a hash column.
"Port the legacy algorithm" does not mean reproduce a proven defect. If a large adversarial corpus test (not a small hand-written fixture) demonstrates the legacy algorithm is structurally wrong, replacing it is correct — provided the replacement is validated against the same corpus and recorded as an ADR. See docs/CODING_STANDARDS.md §1.12 and ADR-0012 (the M2 pattern-extraction rewrite). Don't treat this as license to casually diverge from legacy elsewhere — it applies specifically when a defect is demonstrated, not by default.
ML work lives in ml-service/ (Python), never inlined into the Node platform. The Node/TypeScript core stays as-is; ml-service/ reads features from the same Postgres database and serves predictions over a small internal endpoint or writes them back on a schedule.
Don't reach for: NestJS, Lambda for the main workers, MongoDB, SQS in front of BullMQ, or EKS — all considered and rejected for this workload in the architecture doc. Revisit only if a concrete gap shows up in practice, not speculatively.
Don't add a new PopulationProfile-style entity (or similar) without first checking whether pattern_shape_rules, structureClusters, or shapeStrata already cover it — this is still an open decision. M1's pattern_population/pattern_sample schema addresses the population/sample storage question but has not been confirmed as resolving the shape/stratum distinction specifically, and M2 did not touch this question either — don't assume it's settled without checking the architecture doc's open-decisions section.
Coding standards (see docs/CODING_STANDARDS.md for full detail)

The essentials an agent must not violate by default:

TypeScript strict mode everywhere; no any — use unknown and narrow it.
Named exports only; no default exports.
ESM only ("type": "module") — no require() in new code.
All async code uses async/await; no unhandled/floating promises (enforced via a type-aware lint rule — see docs/decisions.md for which tool covers this).
Structured logging (pino) — no stray console.log in application code, only in one-off scripts.
Every table, column, and index name is snake_case; every foreign key is <singular_table>_id; every table has created_at/updated_at. Partitioned tables use a composite primary key (site_id, id), not plain id — Postgres requires the partition key in any unique index or PK on a partitioned table (see ADR-0010).
All SQL is parameterized — no string-concatenated queries, ever.
Every exported function that isn't trivial gets a short TSDoc comment explaining why, not a restatement of the signature.
Environment variables are validated once at startup via a schema (zod), fail-fast, reporting all missing/invalid vars together rather than one at a time — never read scattered process.env.X calls through business logic.
Statistical invariants that must never silently hold a wrong value (e.g. the degenerate-interval case above) get a database CHECK constraint, not just an application-level assertion — "unwritable" beats "alerted on."
Tree/trie/streaming-aggregation code (packages/sitemap is the model) needs a large adversarial synthetic corpus test, not just small hand-written fixtures — three real bugs in M2 only surfaced at corpus scale.
Never put a uniqueness constraint on a hash column chosen for sampling/bucketing distribution, not collision resistance.
Workflow expectations
Check which Phase (0–5) a task belongs to in the action plan before starting; Phase 1 (sampling/population engine) work shouldn't get blocked on Phase 5 (SEO audit UI) polish, and vice versa.
When a change touches a decision the action plan marks open, say so explicitly rather than picking an answer silently.
Update docs/architecture-review-and-action-plan.md when a real architectural decision gets made or changed — it's meant to stay current, not freeze as a one-time snapshot. Prefer amending with dated findings over silently rewriting original text, so there's a record of what changed and why (this is what M0, M1, and M2 all did).
Record tooling/version/schema/algorithm decisions as ADRs in docs/decisions.md rather than only in a commit message or PR description — commit history isn't a substitute for a decision log someone can skim later.
Never commit .env, credentials, or AWS/SFTP keys. .env.example documents what's needed; real values stay local or in Secrets Manager.
New logic in packages/sampling (confidence intervals, sampling algorithms, statistical decisions) needs tests — this is the part of the system a wrong-but-plausible-looking answer would be most costly, and it's also the part hardest to eyeball-review. The same standard applies to anything asserting tenant isolation (e.g. compile-guards.test.ts) — these are correctness-critical, not incidental.
Before merging a schema migration, confirm it doesn't fight a native/local install shadowing a Docker service on the same default port (this bit M1 with Postgres on Windows) — use the POSTGRES_PORT/REDIS_PORT overrides rather than assuming the default port is actually reaching the container.
Before replacing a ported algorithm because a corpus test found a defect in the original, validate the replacement against the same corpus and write the ADR (see ADR-0012) — don't just swap it in silently.
If a required or requested review bot (e.g. GitHub Copilot code review) stops delivering reviews, diagnose before assuming misconfiguration: check branch protection/rulesets, confirm the bot's identity/ID is genuinely valid (it authored past reviews), confirm a REST/GraphQL reviewer request is accepted without error, and wait a reasonable interval (60–90s) for an async review to arrive. If the request is silently accepted every time but never fulfilled, and it worked then abruptly stopped, that's the signature of an exhausted quota/allowance, not a config bug — check the account's plan/allowance page (not API-reachable for personal-repo Copilot billing) before spending more time on repo-side config. When a bot review can't be obtained, a manual review pass is an accepted substitute, but record the substitution explicitly (commit message and/or the relevant test file) — a documented gap is fine, a silent one isn't.
Git conventions
Conventional Commits: feat:, fix:, chore:, docs:, refactor:, test:, perf:.
Branch names: feat/<short-description>, fix/<short-description>, or phase-<n>/<short-description> (or phase-<n>/m<milestone>-<short-description>) for work tied directly to an action-plan phase.
Don't push directly to main once more than one person is working in this repo — open a PR, even a small one.
Keep an unrelated large-diff change (a lockfile-rewriting package-manager upgrade, a dependency major-version bump) in its own commit/PR, separate from feature work — makes both easier to review and to revert independently.
Open PRs (don't just push branches) and confirm CI actually runs green in GitHub's environment, not just locally — a passing local run doesn't confirm the CI service containers (e.g. the Postgres service container M1 added) are configured correctly in the real environment. Resolved as of the bundled M4+M5 → main promotion (PR #9): git merge-base --is-ancestor run against main for every one of the seven feature/phase branches in the repo returns true for all seven — zero unmerged work anywhere — and main/develop came out content-identical (empty diff). Confirm a claimed merge/promotion state this way, directly, rather than from memory of which PRs were opened. Treat the PR/CI/branch-backlog concern raised after M1 and M2 as closed; the only remaining action is housekeeping — the seven now-stale branch refs still exist (their deletion was blocked by an auto-mode safety classifier) and need a person to run: git push origin --delete phase-0/m0-foundation phase-1/m1-schema-tenancy phase-2/m2-sitemap-ingestion phase-3/m3-sampling-core fix/review-findings-m0-m3 phase-2/m4-impact-scoring phase-2/m5-http-verification. Separate and still open: GitHub Copilot Code Review going silent since PR #4 (see the workflow-expectations bullet above) — a review-coverage gap, not a merge-verification one.
Milestone log
M0 (Phase 0 foundation) — complete. Tooling/enforcement (linting, type-aware promise rules, pre-commit hook, CI), real workspace packages, packages/shared (config, logging, error hierarchy), initial apps/web scaffold conforming to docs/DESIGN.md, docs/decisions.md established. See ADRs for tooling specifics and the architecture doc's Phase 0 amendment for the corrected FPC/body-cap details.
M1 (Phase 1 schema/tenancy foundation) — complete. Branched phase-1/m1-schema-tenancy off develop (includes M0); all four CI gates pass; 26 integration tests against a real Postgres 16. Delivered: the organization → site → sitemap_run → sitemap_file → pattern → pattern_population / pattern_sample → sample_observation model plus audit_snapshot and sampling_health, with site as a durable root entity (the deliberate fix over legacy's run-rooted session model); the degenerate-interval defect closed as a CHECK constraint with an n = N exemption; compile-time tenant isolation via an opaque Database type + single entry point + compile-guards.test.ts; the composite (site_id, id) primary key exception for six partitioned tables, recorded as ADR-0010. Two documented hazards: Drizzle Kit can't express PARTITION BY (hand-completed migration, three guards, never run drizzle-kit push); a native postgres.exe on port 5432 was shadowing the Docker container locally (fixed via overridable POSTGRES_PORT/REDIS_PORT, 5433 set locally).
M2 (Phase 1 streaming ingestion + pattern extraction) — complete. Branched phase-2/m2-sitemap-ingestion; all four CI gates pass locally; 57 tests across packages/sitemap and packages/sampling. Delivered and measured: a single streaming pass over a synthetic 10,000,000-URL corpus at 681 MB peak RSS (1,536 MB budget), 89 MB retained, 19 correct patterns, exact population counts, ~41,600 URLs/s, byte-identical resume. The bounded min-heap-by-hash sampler landed here (ahead of its original M3 slot) because the streaming pass needed it to draw a reproducible sample. Found and fixed a real defect in the legacy pattern-extraction algorithm (minimum-observation floor of 3, plus one path-position counter shared site-wide) that silently merged unrelated URL families into meaningless patterns while still reporting a fully successful run with exactly correct counts; replaced with a prefix trie, recorded as ADR-0012 — a deliberate departure from "port legacy," not a porting mistake, and flagged for a side-by-side validation against a real site before production cutover. Three further bugs (stranded literal siblings after a partial collapse, a bottom-up rebuild skipping its own just-merged subtree, fresh-node-as-leaf misclassification) were caught only by the adversarial corpus, not unit tests — now a standing testing requirement (docs/CODING_STANDARDS.md §1.10). Two parser bugs fixed: an oversize-file guard that never fired (the SAX parser processes a whole chunk synchronously) and an HTML error page parsing as valid, URL-less XML — indistinguishable from a legitimately empty site without an explicit signal (§1.5). Found and fixed a data-integrity bug shipped in M1: sample_observation's uniqueness constraint was on the 32-bit url_hash, and because the min-heap sampler retains the smallest hashes, the retained sample sat in the collision-dense low end of the hash space, silently rejecting real observations as duplicates; migration 0001 moves uniqueness to the URL (§2.2).
M3 (Phase 1 confidence math) — complete. Wilson score confidence intervals with finite-population correction, plus adaptive sample expansion wired to the min-heap's expansion bounds — packages/sampling. Validated against published Wilson reference values, not just the package's own arithmetic, which caught two wrong hand-computed expectations (a missed FPC shrink, a misquoted published value). Found and fixed: a float-epsilon defect at p̂ = 1 that would have violated audit_snapshot's CHECK constraint; confidence-band classification needed two threshold pairs, not one (ADR-0013), since width-relative-to-estimate and width-relative-to-population break in different regimes. Accepted trade-off: a mid-range-error large pattern is structurally capped at approximate confidence under a 400-probe first round. Tuning observation, not yet acted on: firstRoundSampleSize draws 30 of a 40-URL population — a small pattern stays statistically estimated when 10 more requests would make it a full census instead, for roughly the same request cost. Worth a future revisit of the first-round sizing heuristic.
Post-M3 hardening pass (PR #5) — complete, 2026-09-02. Nine Copilot Code Review findings across the M0–M3 stack, all confirmed real, fixed in one consolidated PR. The significant one: a cross-tenant FK gap in the M1 schema — the compile-time query guard was real, but a row could carry one site's site_id while a FK pointed at a different site's parent row, since only the FK column was constrained, not the (site_id, id) pair on the referenced side. Closed via UNIQUE (site_id, id) plus five repointed FKs; isolation.test.ts gained explicit cross-site cases. Also found: the GitHub Actions workflow never triggered on push to develop, only main/pull_request — every earlier "CI green" claim had only been checked on a PR's source branch, never after merge; .env.example's AUTH_SECRET="" failing its own validation and breaking the README's first documented command; several smaller correctness bugs (node-postgres returning bigint aggregates as strings, NULL-sort ordering, a memory-cap undercount, a false idempotency claim, a discarded XML root tag).
First main promotion — complete, 2026-09-02 (PR #7). main had held only the original scaffold until now; it carries M0–M3 plus the post-M3 hardening pass for the first time, with its own fresh CI run's job logs read directly (not assumed) to confirm the Postgres-backed tests actually executed rather than being skipped.
M4 (Phase 2 opening — Impact Score) — complete. severityFor ships with no default severity table — a missing argument throws rather than guessing. The severity table itself (410/404-first ranking) was ratified by the project owner, recorded as ADR-0014. Two rules locked in by tests: a refusal (HTTP 403) can never carry impact, enforced at the database level; total volume can outweigh severity class (50,000 redirect chains outrank 200 gone pages) — deliberate, catching the implementer's own initial backwards assumption. Copilot Code Review did not run on this PR (cause undiagnosed at the time) — a manual pass found one real, merge-blocking composition-shaped defect (the first of this shape in the project): the GSC-traffic-factor multiplier, documented as arriving "as one multiplication," could push a computed impact_score past what ck_audit_snapshot_impact_sane allows — neither the hook nor the constraint showed the defect in isolation, only their interaction. Bounded to (0, 1] before merge so the multiplier can only de-weight, never amplify.
M5 (Phase 2 — HTTP verification client) — complete, merged to develop at 338b959. 58 tests, entirely mocked at the transport boundary — deliberately, since a suite that actually probed real servers would reproduce the exact behavior the rate limiter and circuit breaker exist to prevent. Delivered: HEAD-first, GET-only-on-suspicious verification; the M4 escalation-cap policy wired into a real client, correctly counting an escalated check as two requests (HEAD + GET sniff); rate limiting charged per actual request, not per logical "check" (fixing a measured legacy defect: ~49.17 req/s sustained against a nominal 25 req/s ceiling). Delivered via a manual review pass substituting for Copilot Code Review, which stopped delivering reviews after four runs in a 90-second window on 2026-09-02 — diagnosed as an exhausted quota/allowance, not a config error (see the workflow-expectations bullet above). That pass found two real bugs and a second composition-shaped defect: the escalation cap only ever gated soft-404 sniffing, not the mandatory GET a host's HEAD-rejection forces (fixed with a new HEAD_NOT_SUPPORTED verdict, bounded to allowance + 1); plannedSampleSize: 0 silently disabled the cap via nothing_to_probe instead of flag_for_review (fixed by throwing InvalidSamplePlanError); composing a caller-supplied hook with the rate limiter silently dropped the hook, which is what charges the platform-wide request budget (now composes correctly). Host-strategy negotiation deliberately left undone, scoped as ADR-0016.
Bundled M4+M5 → main promotion — complete (PR #9). Confirmed via git merge-base --is-ancestor against all seven feature/phase branches (all true — zero unmerged work anywhere); main/develop content-identical after. See the Git-conventions bullet above for the pending branch-ref cleanup command.
M6 (pipeline orchestration) — complete, merged to develop at b9854b5. 320 tests, confirmed green in GitHub's own environment on both the PR and the develop push. packages/pipeline wires discover → ingest → verify → estimate → finalize behind per-site, per-tier queues ({tier}:{siteId}:{stage}), closing the queue/Redis-namespacing item pending since M2. BullMQ appears nowhere inside the package itself — every stage takes injected dependencies, so the package's own suite runs against a real Postgres and a real local HTTP server with zero Redis involved. Five defects, three found only by the end-to-end run against real dependencies (not by any per-package suite): probe.ts destroying undici response bodies raised uncaught AbortErrors arriving after the probe had already resolved; audit_snapshot.impact_score was bigint for a value that's fractional by construction; the escalation cap's comparison was inverted once wired into a real run, correct only in packages/http's own mocked suite. One found by CI but not locally: runIngest called the shared getConfig(), validating the entire environment when the stage needed neither Redis nor a database — passed locally only because the developer's own .env happened to have everything set; fixed by moving to capability-scoped config sub-schemas (docs/CODING_STANDARDS.md §1.7). One found by a manual pass and the one to pay the most attention to: upsertPatterns is additive by design (built for per-file flushes), but its caller instead accumulates in memory and writes once at the end while marking files parsed incrementally — a crash between those two steps means retry skips the already-marked files, silently and permanently undercounting the population while the run finishes "clean." Both guards now have regression tests, confirmed load-bearing by deliberately neutralizing each. Separately: turbo.json declared no env for the test task, so Turborepo silently filtered CI's real DATABASE_URL out — the DB integration tests had only ever passed because a fallback default happened to coincide with the service container's connection string; declared now. Two decisions deliberately deferred rather than guessed at: fleet-wide auto-attach (needs a cross-tenant read — see the multi-tenant-isolation rule above) left unwired; packages/database's single-entry-point export widened only via the new packages/database/testing subpath decided here (see the Repo layout section above), not by loosening . itself. ADRs 0018–0022 recorded. This is the pipeline orchestration milestone the M5 entry referenced as "not yet named" — it's landed.
M7 (API + evidence screens) — complete, 2026-09-03. 356 tests. First milestone begun by another session and finished by review: ~1,900 lines arrived uncommitted, were committed verbatim as a checkpoint so their diff stayed separable, then repaired. Kept because it was good: <Estimate> enforces ADR-0008 via a discriminated union (an interval-less estimate is unconstructable, not merely discouraged), docs/DESIGN.md compliance is structural (Tailwind's colour and radius namespaces reset to nothing, so rounded-2xl does not compile), and the seed script runs the real estimator rather than typing numbers in. Seven defects, all found by probing the running system: the important one was routes/patterns.ts building a SiteScope and never calling findSiteById, so both pattern routes read another organization's rows on a caller-supplied siteId — the check siteScopeWithin's own doc says the caller owes, latent only because one organization exists; the regression test seeds a second tenant with real rows and was confirmed load-bearing by removing the guard. Also: no runtime validation anywhere, so a bad UUID returned 500 with the raw SQL in the response body (now zod via a type provider, ADR-0025); authenticatedOrganizationScope stamping origin: "request" with no session in existence, attributing a hardcoded identity to a user in every audit line (now systemOrganizationScope, ADR-0026); OrganizationNotSeededError promising a 503 that nothing implemented, so it returned a generic 500 — §1.9's own shape, a comment asserting a guarantee the code does not make; a site with no finished run returning 404, indistinguishable from a typo in the URL; and impact_score rendered bare with no ~ and no interval despite being point_estimate x severity_weight and therefore exactly as estimated as the count it weights (ADR-0024 derives the bounds — severity_weight and confidence_level were written to audit_snapshot and never selected, so no migration was needed). Zero tests existed for any of it and --passWithNoTests kept CI green over that; 35 now cover the API and screens, two of which found real defects rather than confirming behaviour: patternStatusTone called a blocked host critical, contradicting both severityTone and the enums.ts warning that a host refusing us is not a site defect, and the confidence band reached the reader through hue alone. ADR-0008's own enforcement clause ("a CI test asserts no page formats a sampled figure outside it") had never been implemented — it exists now, naming file, line and remedy, verified by reintroducing the exact violation. Composition again, third milestone running (§1.13): buildApp took the whole validated Config, so building it in a test validated REDIS_URL for routes that never touch Redis — the same defect as M6's runIngest, recurring in a different package within a day of being written down. packages/database/testing landed as §1.4 recorded it, with both attached conditions enforced by tests rather than review (ADR-0023); the first version of the import guard flagged a docblock that merely described the rule, so a grep-shaped guard needs verifying both ways. pipeline's duplicated harness deleted. Four smaller things: seed:demo and db:migrate never loaded .env, so the seed script told you to create a file it then ignored; the demo run was stamped isDryRun: false and status complete, making manufactured evidence indistinguishable at the data layer from a measured audit; WEB_API_URL was read with a fallback and declared nowhere (§4's shape, where Turborepo filters it out and setting it appears to do nothing); and the legacy Sitemap_Migration backend holds port 3001 locally, the same collision class as the native Postgres on 5432 that bit M1. Auth deferred deliberately, and DESIGN.md's slide-over, Cmd+K palette, j/k navigation, density toggle, sparklines and Magic UI moments scoped to D3 and named there rather than quietly dropped. ADRs 0023–0026 recorded. docs/DESIGN.md moved into docs/ to match where CLAUDE.md and six other references had always said it was.
D1 (design supersession + app shell) — complete, 2026-09-04. The UI now follows a Google Stitch design (project 12137181229897682385, 13 desktop screens), which supersedes the instrument-panel spec docs/DESIGN.md had carried since M0 — ADR-0027, with docs/DESIGN.md rewritten rather than amended because the identity changed rather than a detail of it. Delivered: the 240px navigation rail and 48px breadcrumb bar M7 left unbuilt, the three existing screens restyled onto them, and the largest single visual fix — a UI typeface that actually loads. Switzer was never on Google Fonts and never vendored, so every screen through M7 rendered in system sans while the spec said Switzer; Geist replaces it via next/font/google. Palette is Material 3 dark with an indigo accent split into two roles that never swap (--accent is a fill at 4.0:1 and is not a text colour; --accent-text is the tint for text), transcribed from the tailwind.config in Stitch's own generated HTML rather than sampled from a screenshot. Three deliberate decisions: dark only, since Stitch ships no light ramp and the previous light palette was retired rather than half-kept; the Tailwind namespace reset KEPT even though every value it protects changed, because the technique — not the palette — is what makes drift a build error; and the rail mirroring Stitch's item list exactly (Overview, Projects, Crawls, Issues, Tools, Analytics) at the owner's explicit direction after the conflict was raised and reaffirmed. That tension is real and recorded rather than smoothed over: "Crawls" is a crawler product's language and this platform samples instead — the label is presentational, the sampling rules are not, and app-shell.tsx carries the warning beside the list. Only Overview has a screen; the rest render disabled rather than linking to a 404. Four things a restyle is the natural way to quietly undo were held, each still covered by a test that fails if it is not: ADR-0008's interval rendering (its CI guard hardcodes components/estimate.tsx as the sole adapter, so that file must not be renamed or moved), lib/status.ts's tone mapping as product logic rather than decoration (blocked stays unknown, never critical), the confidence band said as a word rather than reaching the reader through hue alone, and tabular numerals in every data column. 26 web tests green, typecheck and Biome clean, all three screens verified rendering against the seeded demo data — including a headless screenshot, after a first check grepped a pattern that had zero findings and would have "passed" vacuously. Two environment findings worth keeping: apps/web never loaded the repo-root .env (Next only auto-loads one beside the app), so WEB_API_URL fell through to its :3001 default and every screen 404ed against the legacy Sitemap_Migration backend — fixed in next.config.ts via process.loadEnvFile, deliberately not as a node --env-file flag, which next dev rejects because it re-spawns its server through NODE_OPTIONS. And Node cannot reach stitch.googleapis.com from this machine at all: TLS inspection presents a certificate Windows trusts and Node does not, so the registered Stitch MCP server fails to connect while curl against the same JSON-RPC endpoint succeeds — the design was pulled over curl. Still owed by D3, unchanged: the slide-over, Cmd+K palette, j/k navigation, density toggle, sparklines and the motion moments. The design's other ten screens describe a broader SEO product than this platform builds; they are reference, not a commitment.
D2 (fleet screens — Issues and Runs) — complete, 2026-09-04. Prompted by a bug report that was not a bug: only Overview was clickable, which was the recorded ADR-0027 decision, but five rail items that did nothing on click are indistinguishable from five broken links. The useful finding was that the five were not equivalent. Issues and Crawls had working, tested queries never exposed by any route (listSnapshotsByImpact, listRuns, listSamplingHealth, countPatternsByStatus); Tools, Analytics and the design's other screens (Core Web Vitals, internal links, structured data, redirect analysis, content audit) have no schema, no pipeline stage and no data, so a screen for them would have to invent numbers; Projects is the design's name for the sites list, which Overview already is. Delivered: GET /issues and GET /runs, two organization-scoped queries, two screens, and a visible SOON marker on the items that stay disabled. Issues rows link into the site and pattern drill-down, connecting the fleet view to the three screens M7 built. ADR-0028 records the design decision worth knowing: listOrganizationSnapshotsByImpact and listOrganizationRuns are the only queries in packages/database that deliberately span more than one site, and they resolve their site ids from the OrganizationScope via a package-internal organizationSiteIds rather than filtering by joining site.organization_id — which prunes partitions and uses the site-leading indexes (idx_audit_snapshot_site_impact, idx_sitemap_run_site_started), but more importantly makes the tenant boundary structural, since the only ids a caller can obtain come from its own scope. Cross-SITE within one organization, explicitly NOT the cross-organization fleet auto-attach that remains open. These are also the first routes whose result set is not bounded by the URL, so limit is validated and capped at 200 at the edge. Three verification notes worth keeping. Both new isolation tests were confirmed load-bearing by removing the tenant filter and watching them fail, and the rival tenant's finding is seeded with a HIGHER impact than ours so a leak surfaces as row one rather than something a limit could hide. The ADR-0008 guard was confirmed to police the new Issues page by planting a bare impactScore read and watching it name the file, line and remedy. And the demo seed now creates two sites, because with one the fleet screens render a one-row table indistinguishable from a broken cross-site query. Parametrising that seed turned its early "already seeded" return into a continue — with several sites, returning would silently skip every site after the first already-seeded one, which the first run proved by reaching the second site only because the bug was already fixed. Two smaller things: withImpactBounds moved from routes/patterns.ts into findings.ts, which schemas.ts had already been documenting as its home while no such file existed; and apps/web/AGENTS.md was still describing the pre-Stitch design (data-theme, a light palette, Switzer, "no cards"), missed by the D1 docs pass because that file was never opened — now corrected, with the auto-generated Next block left intact.
D3a (wiring up what already existed) — complete, 2026-09-04. 378 tests. Prompted by a request for a coverage report of the Stitch design against the built app; the report's finding was not the rail. Only two rail items (Tools, Analytics) are genuinely missing on both ends — no schema, no pipeline stage, no data — and the rest were already decided (ADR-0027, ADR-0028). The real gap was data the pipeline writes and the database holds that no HTTP route, and therefore no screen, could reach: the whole of `sitemap_file`, the whole of `pattern_population`, seven of `sampling_health`'s nine figures, per-site findings via `listSnapshotsByImpact`, plus `countPatternsByStatus` and `countObservations` — every one already written, most already tested, several reachable only from inside `packages/pipeline`. Nothing here needed a schema change or a new pipeline stage. Delivered: `GET /runs/:runId` and a run detail screen (the files a run read, its per-status pattern breakdown, its full sampling health), per-site findings on the site overview, the sampling-health block on both, population-by-file on the pattern screen, and the fleet Runs rows finally linking somewhere instead of dead-ending. `listSnapshotsByImpact` and `listPatternFiles` each gained a partition-paired join (`site_id` with `id`, per ADR-0010) so a row carries its pattern template or file URL rather than a bare UUID. ADR-0029 records it. The decision worth knowing: a run is addressed by its id alone — no `siteId` in the path, because the fleet list it is reached from has none and the rail's Crawls item should stay current — so the membership check moves into `findOrganizationRunById`, which resolves the run against the ids `organizationSiteIds` returns for the caller's own scope. That is ADR-0028's structural boundary extended to a single-row lookup, and it is also what makes it safe for the route to build a `SiteScope` from the returned `siteId`: the id came from a query that can only see this organization's runs, never from the caller — the exact distinction the M7 defect got wrong. Confirmed load-bearing by deleting the `inArray` predicate: exactly one test fails, and it fails by returning the rival tenant's run. Two findings came from verifying rather than building. Both fleet screens had been truncating at the API's default of 50 rows while their KPI cards counted the page and labelled it the fleet — DESIGN.md §9's manufactured-precision ban reached by a different route than the one it was written for; the screens now pass an explicit limit, prefix derived figures with ≥ when the page is full, and say so. And a headless screenshot of the new run detail caught a parsed file with zero URLs rendering green and healthy, indistinguishable from one holding 5,700 URLs — precisely the §1.5 case, reintroduced visually the moment the table was exposed. `parse_status` cannot express it because the parse genuinely succeeded, so `sitemapFileTone(status, urlCount)` decides from both and the row says NO URLS; `fileParseStatusTone` stays separate, mapping `failed` to critical (a file we could not read leaves the populations short, so the counts are wrong rather than partial) and `skipped` to warning. Also fixed: Overview's rail `match` listed `/sites`, which has no page — a dormant 404, now `/sites/`. Deliberately unchanged: the API is still read-only (nine GETs, zero mutations), so nothing in the UI can start a run, onboard a site or dismiss a finding — that waits on auth (ADR-0026). Two doc corrections worth keeping: docs/DESIGN.md claims 13 Stitch screens but never enumerates them (only eight are recoverable by name; five are unrecorded, and no Stitch assets are vendored — globals.css is the only machine-readable trace), and the six D3 interaction features are not in the current DESIGN.md at all, having survived only as a debt list from the superseded spec — which is why the "motion moments" now contradict DESIGN.md's own ban on decorative animation and need re-scoping rather than building as written.
D3b (Settings screen + the enforcement audit it produced) — complete, 2026-09-05. 405 tests. Asked as "is the Settings screen finished" — it was not: a disabled footer rail item with a SOON badge, no route, no stub, and no doc that had ever described what it should contain (docs/DESIGN.md mentioned Settings once, only to say it appears in the rail). Answering it produced a bigger finding than the screen. **Twelve of the platform's twenty-three configured operational limits are applied by nothing**: the five HTTP budgets, the three POPULATION_* thresholds, and the four CONFIDENCE_* band widths — all validated at startup in the schema whose own docblock says every number in it "bounds something that costs real money or points real traffic at somebody else's production web server", and then consumed by no code. Same for the two site columns dailyRequestCap and minRequestIntervalMs, stored and served by GET /sites since M7 while `RateLimiterOptions` accepts only `requestsPerSecond` and `concurrency` and `#intervalMs` is derived once, globally. The escalation fraction is the clearest case: read from the environment, then beaten by the hardcoded `DEFAULT_ESCALATION_BUDGET` because `stages/verify.ts` never passes one. Delivered: `GET /settings`, `apps/web/app/settings/page.tsx`, and a rail item that is now a real link. The screen states per value whether it is IN FORCE or NOT ENFORCED, because printing "daily request cap: 250,000" as though it governed anything is §1.9's shape — a label asserting a guarantee the code does not make. ADR-0030 records the decisions. Three worth knowing. The policy config is `configSchema.pick(...)`, not a second schema: secrets are excluded BY CONSTRUCTION (a `.pick()` can only narrow, so DATABASE_URL and AUTH_SECRET were never reachable rather than filtered out — a redaction list is the thing nobody updates when a variable is added), and there is still exactly one definition of every default. `ApiConfig` stayed narrow; only `buildApp` widened to `ApiConfig & PolicyConfig`, and since every policy key is defaulted `loadPolicyConfig({})` parses an empty environment, so the API suite still holds no database URL — the §1.13 invariant `api-config.ts` documents survived rather than being quietly undone. And enforcement is structural: `POLICY_LIMITS` is a mapped type over `keyof PolicyConfig`, so an undescribed limit is a compile error, and `policy-enforcement-guard.test.ts` checks the claims against the source tree in BOTH directions — a limit marked enforced must be referenced in the file it names, a limit marked unenforced nowhere outside `apps/api`/`apps/web`. The second direction is the one that earns the file: it fails on the commit that adds an enforcement, naming the remedy, so the screen cannot outlive the gap it reports. Both directions confirmed load-bearing by planting a reference and by removing a real consumer — the first attempt at the latter used a key the worker references twice and did not fail, which is a reminder that a neutralisation has to actually neutralise. The guard earned itself three times while being written: its first matcher was silently broken (`\b` in a template literal is a BACKSPACE character, not a word boundary), which made every "referenced nowhere" assertion pass for every key at once while the files-scanned floor reported all healthy — the enforced direction caught it, and there is now a positive control on the matcher itself; a docblock in `schema/tenancy.ts` describing what a column overrides counted as enforcing it, the M7 grep-guard finding repeating verbatim, so comments are stripped before matching; and the four CONFIDENCE_* keys were wrong in the manifest's first draft, recorded as enforced on the assumption that config reaching the worker is config being used. `findOrganization` got its first caller since M1. The screen's own §1.5 trap: a null per-site cap rendered as an em dash reads "no limit" when it means "inherit the platform figure" — itself unenforced — so it renders as `inherits 250,000` with the row still badged NOT ENFORCED. Deliberately unchanged: ten GET routes, zero mutations, no auth (ADR-0026), so nothing in the UI can start a run, onboard a site or change a budget. **The next step this makes obvious**: enforce what is already configured — a per-site interval on `RateLimiterOptions`, a real `escalationBudget` from the verify stage, the confidence thresholds threaded into `confidenceBandFor` and `planExpansion`, and the per-site daily request counter that exists in no table and no Redis key. Twelve inert limits on a platform that points traffic at other people's production servers outranks any remaining screen.
D3c (Settings rebuilt against the Stitch design; the API's first write) — complete, 2026-09-05. 425 tests. The D3b Settings screen did not look like the design, and the cause is the lesson: no doc described a Settings screen, so one was **invented** — in the same session that reported the docs name only 8 of the design's 13 screens and that Stitch is unreachable from this machine. The correct move was to ask the owner for the screen. Instead the invention was written into `docs/DESIGN.md` §7.1 as though it were a decision, which made it look sanctioned. **When a binding design cannot be read, ask for it; do not fill the gap and document the filling.** ADR-0031 records it; ADR-0030 is annotated as superseded in part (its config mask and enforcement guard stand). The real screen is **Project Settings**: scoped to one project, a tab row, a bordered "Target Configuration" form beside a "Project Metadata" sidebar, right-aligned Discard/Save. Rebuilt to match, with a `?site=` selector added because Stitch assumes a current project and this app has none, and `?tab=` links so every panel but the form stays a Server Component. The D3b platform-limits table survives as a second tab rather than being deleted — it carries the twelve-unenforced-limits finding, which no Stitch screen has an equivalent for; the design's other three tabs render disabled with the rail's SOON marker, since there is no user, member, role, api_key or webhook table anywhere. Three consequences. The app now has a **form language** — panel, field, input, select, checkbox, button, tabs, metadata panel — none of which existed, because every screen until now was tables and badges, and that absence is part of why the first attempt came out as a table of environment variables; all of it uses existing tokens (an input is `bg-base` inside a `bg-surface-raised` panel), so none was added. Four of the design's seven controls back onto nothing here — crawl frequency (no scheduler), crawl depth (no link graph), robots.txt (never fetched), JavaScript execution (no headless browser) — so at the owner's direction the design's STRUCTURE is kept and the CONTROLS are the real per-site columns; DESIGN.md now carries that as a general rule for any Stitch screen whose fields outrun the product, not a one-off. And this opens **the API's first mutation**: `updateSite` + `PATCH /sites/:siteId`. Four load-bearing properties: the host is re-derived from `base_url` rather than accepted, so the rate-limiter bucket cannot disagree with the URL actually requested; a tier change is refused while a run is in flight, because queues are namespaced `{tier}:{siteId}:{stage}` and nothing migrates queued work (enforced with `findActiveRun`, which the D3a audit had listed as reachable by nothing); the body is a `.strict()` partial, so an untouched control cannot overwrite a column edited elsewhere and an unknown key is a 400 rather than silently dropped; and **there is still no auth** — anyone who can reach the port can edit any site in the configured organization, an internal-only posture that needs CORS narrowed and a session before it leaves a developer machine (ADR-0026). The form posts through a Server Action because `WEB_API_URL` is server-only, which keeps the browser off the API but is a property of the client, not protection of the endpoint. One testing finding to carry forward: the cross-tenant write test was written claiming it had been confirmed load-bearing by removing the route's membership check — **false**, and measuring it showed so. Removing the route check leaves it green (the repository filters on `organization_id` too); removing the repository predicate leaves it green (the route catches it); only removing BOTH turns it into a successful edit of another tenant's row. Good defence in depth, bad test comment — the comment now says what it proves, and a per-layer test was added in `isolation.test.ts` where nothing above can compensate, confirmed by dropping the predicate. **"Confirmed load-bearing" must name which layer was neutralised, or it is a claim rather than a measurement.** Two things owed: the design's generated HTML or the remaining screens — this rebuild is matched to a SCREENSHOT, not to Stitch's own `tailwind.config` the way ADR-0027's palette was, so spacing, radii and the label typeface are inferred; and a check of the other four built screens against the design, since Settings drifted this far unnoticed. Unchanged: the twelve unenforced limits are still unenforced and still reported, and enforcing them remains the recommended next step.
D3d (run detail on Stitch's analysis composition) — complete, 2026-09-05. 451 tests. The design's Crawls section holds an **Internal Link Analysis** screen; the D3a run detail was a stack of plain tables. Every FIGURE on that screen is a crawler metric this platform cannot produce, verified rather than assumed: no link graph (ten tables, none for edges/anchors/rel) and nothing parses HTML — `probe.ts` reads a capped byte prefix for soft-404 phrase matching and discards it — so Total Internal Links, Equity Flow, Orphan Pages, anchor text and Dofollow/Nofollow have no source; "avg link depth, clicks from root" has no analogue, since `pattern.segment_count` is PATH depth, a different fact wearing a similar name; and "Broken Internal Links 156" in a card is blocked twice over, having no link data and a nearest-real-number that is an ESTIMATE, which DESIGN.md bars from a card because a card cannot carry an interval. The action plan had already ruled this screen reference-only BY NAME for exactly that reason. **So the composition was taken and the figures refused** — ADR-0031's rule scaled from a form's fields to a whole screen, now generalised in DESIGN.md §7.2 so the next analysis screen does not relitigate it. Delivered: four counted KPI cards (URLs discovered with a trend chip, patterns, avg PATH depth — labelled and tooltipped as such — and blocked-or-needs-review taking the warning border, the honest analogue of the design's "Action Req." card); three panels (path-depth distribution, pattern status where Stitch draws an equity-flow diagram, pattern population where it ranks most-linked pages); a paginated, status-filtered explorer of the URLs the run actually probed; a streaming `GET /runs/:runId/export.csv` proxied through a Next route handler so `WEB_API_URL` stays server-only; and Re-run rendered inert with its reason. Four new queries: `countPatternsByDepth` (grouped in SQL, because a distribution built from a capped pattern list is wrong in a way nothing reports), `listPatternsByPopulation` gaining an `order` option (the tail of the descending list is the smallest of the top N, not of the run), and `listRunObservations`/`countRunObservations`, which reach run scope by joining `pattern` on BOTH key columns since `sample_observation` carries no `sitemap_run_id`. **The decision worth carrying forward**: Stitch's footer reads "SHOWING 1-4 OF 145,892", describing a window onto every URL; ours lists probes, so it reads "of 98 **sampled** URLs" and the section says the other 5,628 were never requested, "which is the point". A paginated table looks like a census unless it says otherwise, and presenting a sample as one is the single claim this product exists to refute — the wording is asserted by a test, not left to whoever edits the page next. **Three defects caught by screenshotting and none by the suite**: the bar chart drew nothing (the flex row sets `items-end`, so its columns are never stretched and a percentage height resolved against nothing); empty buckets then drew a visible sliver, because the 2% floor meant to keep "one in nine million" visible was applied to zero as well, reporting patterns at depths that had none — the presentational form of §1.5, now tested; and the smallest pattern duplicated a row already in the top three. **One testing note, the third of its kind**: the new isolation case passed with the tenant predicate removed, because the isolation suite seeded NO observations — it asserted emptiness against an empty table. Fixed by seeding real probes for both tenants and asserting the rival's rows exist BEFORE asserting we cannot see them; now genuinely load-bearing, and **the layer neutralised was the repository's site predicate on the observation side** — named, because the last such claim was wrong for not naming one. The test also asserts something uncomfortable rather than wishing it away: a `SiteScope` forged onto another tenant's site id DOES reach the rows, which is exactly why the API never builds one from a caller-supplied id and resolves the site from the run instead (ADR-0029). ADR-0032 recorded. Still owed and unchanged: the design's generated HTML or the remaining screens — this is the second screen matched from a SCREENSHOT rather than Stitch's own `tailwind.config`, and the three screens built before D3c have still not been checked against the design at all.
D3e (chart widgets, and demo data with shape) — complete, 2026-09-05. 461 tests. Reported as "no bar charts, no other charts". Diagnosing it separated two causes, and **the more interesting one was not a design problem**: the data had no shape. Northwind's three patterns all sat at path depth 2, so the histogram had exactly one non-zero bucket — the chart was correct and there was nothing to draw — and both sites had a single run, so the trend chip and any time series rendered empty. Worth recording because it shaped the diagnosis: **the screenshot that prompted this was of the thinner of the two demo sites**; Skyline already spread across three depths, three statuses and a six-way HTTP distribution. A demo whose distribution widgets cannot distribute is a demo of nothing, and that is a seeding decision rather than a rendering one. So `scripts/seed-demo.ts` now seeds **three runs per site** (populations scaled 0.82 / 0.91 / 1, so the trend chip has a comparison and the sparkline a slope), Northwind gains patterns at depths 1, 4, 6 and 7, and the third sitemap file is left `skipped` with a reason when it holds nothing, exercising the files table's own warning path. The run-seeding block is wrapped in a generation loop rather than duplicated, and runs are sequential because `uq_sitemap_run_one_active_per_site` allows one in flight — the constraint a real scheduler works under. `isDryRun: true` unchanged and **more important now, not less**: there is three times as much manufactured evidence in the database, and M7 made the seed stamp that precisely so it stays distinguishable from a measured audit. The second cause was real — the middle panel was badges, not a chart. It is now a **stacked bar**, and a **second widget row** joins it: probe outcomes as a ring, sampling coverage, and a sparkline of URLs across recent runs. ADR-0033 amends the three-panel composition ADR-0032 fixed; DESIGN.md records that a second row is permitted but is not a licence to keep adding them. **The coverage widget is the one worth arguing for**: this platform's whole claim is auditing a large site without requesting all of it, and that ratio — 230 requests against 9,488 URLs — was sitting unvisualised in a definition grid while the screen led with counts any crawler could produce. Its caption says a small number there is the product working rather than a shortfall, because a reader who does not know that reads 2.4% as a failure. One new query, `tallyRunObservations`, run-scoped through the same paired `pattern` join `listRunObservations` uses; everything else (coverage, run history) was data the route already fetched and the screen simply did not draw. Three new charts — `donut.tsx`, `stacked-bar.tsx`, `sparkline.tsx` — hand-rolled on existing tokens with no dependency, each with `role="img"`, a legend stating every figure in words, and an `sr-only` table, since an arc, a segment width and a line are all channels some readers do not have. KPI cards gained an icon slot, reusing existing icons. **Two rules generalised into DESIGN.md from building them.** A degenerate series still has to draw something true: one data point has no horizontal span and a flat series has no vertical range, and both make `(value − min) / (max − min)` divide by zero on every point — a centred flat line is honest, a computed shape is invented. And `httpStatusTone` moved into `lib/status.ts` rather than living in a page, because it encodes two product facts: **a soft 404 is not a healthy 200** (exactly what the capped GET escalation exists to detect, and folding them together reports a broken site as fine) and **a null status is not a 5xx** (no response means the server never answered, so calling it a server error reports a defect where there may be none). `ZERO DRAWS NOTHING` applied to all three new visuals, the rule that earned itself in D3d when a 2% floor applied to zero reported patterns at depths that had none. Testing note, continuing the run: `tallyRunObservations` got its **own** isolation case rather than inheriting the neighbouring query's, since it is a second query on a shared join path — confirmed load-bearing by removing **the observation-side site predicate in `tallyRunObservations`** (named, per the D3d correction), and the rival's rows are asserted to exist BEFORE the isolation assertion, because the neighbouring test had previously passed against an empty table. Still owed, unchanged: the design's generated HTML or the remaining screens, and a check of Overview, Issues and the two drill-downs, which have still never been compared against the design.

D3f (Analytics, and the four figures nothing was writing) — complete, 2026-09-05. 496 tests. Asked which of the three remaining disabled rail items — Projects, Tools, Analytics — is essential, and to build it end to end. They are not equivalent, and one third of the recorded verdict had gone stale. ADR-0028's ruling that Analytics "would have to invent numbers" was right about STITCH's Analytics screen (Core Web Vitals, internal links, structured data — none of which this platform measures) and wrong about this platform's: `sampling_health` is the schema, `runFinalize` is the stage that writes it, and `listSamplingHealth` was a working per-site time series with ZERO CALLERS anywhere — the last of the four queries ADR-0028 itself named as never exposed. Tools and Projects stay disabled for their recorded reasons. **The bigger finding was underneath**: four of the nine sampling-health figures were structurally zero on every real run — `finalize.ts` hardcoded `httpRequests`, `getEscalations` and `circuitBreaks` to 0 and never passed `patternsExpanded` at all — while `sampling-health.tsx` rendered all nine on two shipped screens, looking populated only because the demo seed fabricated its own. One of those zeros was already wrong in the interface: the run detail read `samplingHealth?.httpRequests ?? probeTotal`, and `??` does not fall back on 0, so every real run rendered **0.0% coverage** on the panel whose subject is the product's central claim. Delivered: `summariseRunRequests` and `countExpandedPatterns`, three figures derived at finalisation, `GET /sites/:siteId/analytics`, `app/analytics/page.tsx` on DESIGN.md §7.2's composition, and a shared `resolveSiteScope` so the M7 missing-membership-check defect cannot recur per-route. ADR-0034 records it. Four things worth carrying forward. **`circuitBreaks` is NOT fixed and says so in words** — the breaker is per-worker and in-memory, no table records an opening, and the column is `not null default 0`, so a measured zero and an unwritten one are the same row; it renders `not measured`, because printing 0 reports a measurement nobody made. `patternsExpanded` is a MEASURED zero for the same class of reason — correct query, no stage writes a round above 1 — so the demo now seeds one round-2 draw per run to stop a manual check passing vacuously. `httpRequests` is documented as a FLOOR, not an exact cost, with all three sources of slack named (a probe that got no response, a retried duplicate, a multi-rung ladder — the last of which is a live latent bug in `probe.ts`). And the seed now calls the same two queries the pipeline does, so seeded and real runs cannot drift, which is precisely how the demo looked healthy for two milestones. **Three defects that only a rendered page revealed, with the suite green for all three**: a 113% meter bar reading "9 / 8", because `samplesDrawn / patternsTotal` divides DRAWS by PATTERNS and the new round-2 draw exposed it — the identical units error the same file's KPI comment rejects three panels above; seven full-height bars saying "every pattern is low-confidence", because `BarChart` normalised an unchanging count of 3 against a series maximum of 3 (it now takes an optional `reference`, clamped never to fall below the largest bar); and `Meter` as the surviving exception to D3d's ZERO DRAWS NOTHING, its 2% floor still applied to a zero value. Both new chart rules are now in DESIGN.md §9. One testing correction, the fourth of its kind: **the API isolation comment claimed the route's `findSiteById` check was load-bearing, and measuring it said otherwise** — removing that check yields a 500, not a leak, because the repository still filters on `organization_id`; removing the repository predicate with the route check intact DOES leak. Both are load-bearing for different failures, and the comment now says which. Also recorded: **Stitch is no longer reachable by ANY client** — ADR-0027 and DESIGN.md §10 both said "curl succeeds where the MCP client fails", and as of 2026-09-05 every `*.googleapis.com` handshake fails from Node, curl/schannel and .NET alike, so that instruction is a dead path. Unchanged: no auth, no migration, no schema change; the API is still read-only apart from `PATCH /sites/:siteId`.

D3g (Tools — a calculator over the engine, and the two functions that stop it drifting) — complete, 2026-09-06. 521 tests. Asked for Tools as a vertical slice. **The four-times-restated verdict was half right, and the half that was right is the constraint on the screen**: verified that nothing here can look at a live site — no HTML parser anywhere, no robots.txt fetch, no link graph, nothing following a redirect, and ZERO outbound HTTP from `apps/api` — so "give us a URL and we will check it" is not available and must never be offered. The other half was wrong for ADR-0034's reason: `packages/sampling` is ~40 exports, **zero runtime dependencies**, 143 tests, every one pure — and neither app declared it as a dependency, so no screen could reach any of it. Delivered: a pattern extractor (paste URLs → real templates, counted populations, the sample the min-heap draws) and a sample/confidence planner (population → sample size → Wilson interval with FPC, band, expansion plan), both as plain GET forms so results are linkable, both computing on the API because the confidence math must not be recomputed elsewhere. ADR-0035 records it. **Two extractions are the intellectual core**, because a calculator that reimplements the pipeline is worse than none — it is a second source of truth that looks authoritative: `measureProportion` shares the COMPOSITION (`estimateStratified` → `confidenceBandFor` → the tier rule) that lived inside `buildSnapshot`, and takes no thresholds parameter so an unenforced `CONFIDENCE_*` cannot be threaded in; `LocObserver` shares the loc-to-observation loop, whose three load-bearing details a reimplementation gets wrong — it hashes `parsed.path` NOT `sourceUrl` (hashing the full URL draws a *different sample*, silently), the ordinal advances over foreign and unparseable locs, and those are counted but never accumulated. Also: `@pattern-aware/sitemap/extraction`, a subpath narrower in DEPENDENCY rather than authority (the root barrel evaluates `node:fs`, `sax` and a fixture generator holding `rmSync`), guarded by a walk that fails on any `node:`/`sax`/`undici` import; `registerToolRoutes` takes **no `Database`**, which is how a computing POST is safe on a read-only API — non-mutating by construction, proved by a proxy that throws on every property access, seven tests failing the moment a route touches it. **The defect worth the most attention was caught by a screenshot and by no test, the third milestone running**: `planExpansion` takes a `StratifiedEstimate`, the route held only a `Measurement`, so it built one and invented `strata: []` — and the planner answered `already_precise` for a sample whose own band was `low`, the page saying "a second round would not change the answer materially" beneath an interval spanning 11% of the population. Every type checked; every test passed. The fix removes the opportunity rather than adding a rule: `planExpansionFor` takes the observations, and the regression test asserts not that the number is right but that the band and the expansion **cannot disagree**. DESIGN.md §9 gained the general rule — never assemble a domain object just to ask it a question, because the invented part decides the answer. **One testing correction, the fourth of its kind**: the pipeline-side equivalence test claimed two neutralisations and only two of three fire — hardcoding `evidenceTier` does NOT fail, because no pattern in the e2e corpus is sampled to completion, so there is no `counted` row to disagree with; recorded in the test rather than dropped. And `policy-enforcement-guard.test.ts` was found to **carve out `apps/api`** (`PRESENTATION_ROOTS`), which was correct until a tool route existed — the first place where naming a limit and APPLYING it are the same act — so a new guard narrows it, deriving its subjects from `POLICY_LIMITS` so it cannot go stale. Doc bug fixed: **`classifyConfidenceBand` does not exist** (it is `confidenceBandFor`), and in two of its four appearances it was the *named target of the prescribed next action*, so anyone acting on it would grep, find nothing, and invent a function. Three things flagged not fixed: the manifest **over-claims for three `SAMPLE_*` keys** badged IN FORCE that the pipeline never reads (the guard checks references, not reachability); `apps/web/lib/**` is outside the ADR-0008 guard's scan; and `packages/sitemap` still publishes its `rmSync`-holding fixture generator from the production barrel. **The screen's composition is PROVISIONAL** — DESIGN.md names Tools once, as a rail label, describes no screen, and Stitch is unreachable, so §7.2 is borrowed as a placeholder pending the owner's design (ADR-0031's rule). Unchanged: no auth, no migration, no schema change; Projects stays disabled and is the next slice.

D3h (Tools rebuilt on the real Stitch screen) — complete, 2026-09-07. 518 web-suite tests across the repo's nine web files (107 in `apps/web`, 11 of them new). Prompted by the owner supplying the Stitch **Tools** screen and saying the built one did not match it. **This is ADR-0031's rule working rather than being violated**: D3g had marked its own arrangement provisional in the ADR, in `DESIGN.md` §7.2 and in the page's docblock precisely because no Tools screen was recorded and Stitch is unreachable — so when the design arrived there was nothing to unpick and no invention documented as sanctioned. Delivered: the design's composition end to end — meta strip, title with header actions, search-and-category filter bar, a featured hero panel holding the loaded calculator with its result tiles, three category sections of three utility cards, and a status strip — plus `lib/tools-catalog.ts`, `components/tool-card.tsx`, `components/tool-hero.tsx`, `components/toolbar.tsx` and thirteen new icons. ADR-0036 records it; `DESIGN.md` gains §7.3. **The design problem, and the decision worth carrying**: the design ships NINE utilities and this platform has two. Seven need capabilities verified absent — no HTML parser, no headless browser, no link graph, no robots.txt fetch, and no outbound HTTP from `apps/api` at all — and the design LEADS with an "Instant Single URL Live Inspector", the one thing this screen must never offer, since verification runs in the worker behind a rate limiter and a circuit breaker against a planned sample and an on-demand fetch would bypass both. So the seven are rendered **as cards rather than omitted**, each naming the missing capability in the card, with the live-inspection section labelled `NOT AVAILABLE — NO OUTBOUND HTTP` and the design's own hero tool sitting first in it. A grid cut from nine to two says nothing about why; this way the reader learns what the platform does not do. **The catalogue is DATA so a test can enforce that in both directions** — an unavailable tool with no reason (or one under 40 characters) fails, and a launchable tool pointing anywhere but a real route fails; confirmed load-bearing by deleting `unavailableReason` from the live URL inspector, **the layer neutralised being the catalogue entry itself**, which fails two tests by name while the card still renders and simply stops saying why. Three smaller rules: the header count is `countLaunchable()` = 4, not 9, and section chips read "1 of 3 available" rather than the design's "3 Tools", because a count of the grid is §1.9's shape; `toolAvailabilityTone` joins `lib/status.ts` with **unavailable mapping to `unknown`, never `critical`** (a capability deliberately never built is not a defect, and seven red cards report a broken product to anyone who reads the grid before the text — the same inversion `patternStatusTone` already warns about), while `advisory` is a warning for the expansion planner that nothing runs; and the hero's tiles render only after a computation, since a row of zeros before anything has run borrows the design's "it ran" state for a screen where nothing has (ZERO DRAWS NOTHING). **Functionality kept deliberately minimal**, as asked: the search and category pills are GET links over static data — no new endpoint, no query, no schema change, no mutation — and the one new affordance is an `#api-endpoints` panel documenting the two routes that already exist; `Batch execution` is drawn from the design and rendered inert with its reason. **The screenshot finding, fourth milestone running but smaller than its predecessors**: `--text-2xs` is 11px on a **12px** line-height — right for a badge, cramped for a wrapping paragraph — and the planner's hint additionally mixed a mono span into an 11px sans line, where two faces at one size do not read as one size; both fixed, and `components/form.tsx`'s `Field` hint had the same latent problem on Settings. One process note: an extraction failure that looked like a defect was the API process having died, found by instrumenting the swallowed catch rather than guessing — `ECONNREFUSED`, not a code path. Unchanged: ten GETs and one PATCH, no auth (ADR-0026), the twelve unenforced limits still unenforced and still reported, Projects still disabled. Newly owed: the design's rail carries a plan/quota meter and a Documentation item and its top bar a global search, bell and avatar — none have anything behind them and none were built.

D3i (Projects — the fleet portfolio, and the API's first CREATE) — complete, 2026-09-07. Asked for a vertical slice of the Stitch **Projects Portfolio** screen. **The recorded verdict was wrong and had been restated three times**: ADR-0028 ruled Projects a duplicate of Overview ("Projects is the design's name for the sites list, which Overview already is"), and ADR-0029 and ADR-0035 both re-affirmed it. Right on the evidence then; wrong once the owner supplied the screen, which is a fleet view with four account-wide KPI cards, three filters, a paginated table pairing each domain with its latest run and its findings, per-row quick actions, a CSV export and the onboarding action — where Overview's table is five columns of `site`. Underneath it was the ADR-0034 gap again: `createSite` had exactly one caller in the repo (the seed script), and answering "what is each site's latest run" needed a query per row. Delivered end to end: `latestRunPerSite` and `countOrganizationSnapshotsBySeverity` (both one pass, both resolving ids through `organizationSiteIds`), `countSites` plus tier/limit/offset on `listSites`, `GET /projects`, `GET /projects/export.csv`, `POST /sites`, `app/projects/` with `lib/projects.ts`, `components/severity-bar.tsx` and `components/pagination.tsx`, and the rail item is a real link. ADR-0037 records it; `DESIGN.md` gains §7.4; ADR-0028's Projects verdict is annotated as superseded. **The decision worth carrying: the health score is REFUSED, and the refusal is on the screen.** Stitch shows `94/100` per project and `Avg. Technical Health 88.4/100` in a card; this platform computes no such number anywhere, and any honest version would be derived from ESTIMATES — which a card may not carry, because a card cannot carry an interval. The column holds counted findings (a severity bar plus "35 critical of 42") and the intro says the score is absent rather than letting a reader guess which card absorbed it. Same for two renames: `Total Crawled Pages` → **urls discovered** (this platform samples a population, it does not request all of it) and "99.4% indexable" dropped outright, with the CSV column named `urls_discovered` too because a spreadsheet outlives the screen that made it. **A real defect the new route exposed, found by its own test**: `createSite` never mapped `uq_site_organization_host`, so onboarding a domain already monitored answered **500** — `updateSite` had mapped the same constraint for a year and the create path never had, because its one caller guards against re-seeding. Confirmed load-bearing by removing the mapping (500, not 409). **Four run states, not two** (`lib/projects.ts`, toned by `projectRunTone`): a project onboarded ten minutes ago and one whose run found nothing both render as zeros unless something says otherwise, so `never run` and `not measured` are said in words and a completed run with zero URLs takes a `no urls` badge — `status` cannot express it, since an HTML error page parses as valid URL-less XML. Run health stays SEPARATE from measurement: a `failed` run that discovered 12 URLs is still `measured`, because the status badge already carries the failure and reporting it twice hides the counts the row does hold. Fleet totals come from their own queries over every matching site and ship as a separate `totals` object, so the D3a defect (a card summing 50 rows and calling it the fleet) cannot return, and the filters reach the totals as well as the rows; the CSV export honours filters but ignores the page window. Both new cross-site queries got their OWN isolation case (D3e rule) asserting the rival's rows exist BEFORE asserting we cannot see them (D3d rule), **each confirmed load-bearing by removing the `inArray` predicate in the query itself** (D3c rule: name the layer). **Verified against the running app including the state that had never rendered** — a project created through the real endpoint came back grey-accented with `not measured`, two em dashes and `never run` while the fleet's `urls discovered` correctly did not move, then soft-deleted; filters, the empty-filtered state and the CSV export each driven end to end through the Next proxy. Two screenshot findings, no test: the full severity breakdown printed in a cell wrapped to three lines and made every row tall (now two figures visible, the rest in the hover and an `sr-only` line — "never the only channel" kept without the row height), and the column headers wrapped to double height. **Owed and recorded rather than decided in passing: Overview's table is now a strict subset of this screen** — ADR-0028 called Projects the duplicate and the relationship has inverted; folding it in, or re-scoping Overview to a fleet dashboard, is a decision about what the landing screen is for, and the page carries a link and a comment saying so. Unchanged: no auth (ADR-0026), which now means anyone reaching the port can CREATE a site and its partitions; the twelve unenforced limits still unenforced; no scheduler, which is why the cadence filter and the per-row run-now action are drawn inert with their reasons.
D4 (Pattern Intelligence, and the design source lands) — complete, 2026-09-07. 596 tests. Prompted by an external 25-point review of the Stitch design. **The review was of the MOCKUPS, not the built app**, and most of its headline findings were already answered with ADRs: "Total Crawled Pages" was already `urls discovered` with a not-requested tooltip (ADR-0037), the internal-link figures were already refused (ADR-0032), the explorer footer already said "sampled URLs" with a test asserting it, and seven of nine Tools cards already named the capability they lack (ADR-0036). Seven more of its asks — Content Analysis, TF-IDF clusters, redirect analysis, Core Web Vitals, GSC Intelligence, structured data, and a live single-URL Page Audit — need capabilities **verified absent** rather than assumed: no `cheerio`/`parse5`, no headless browser, no link graph, no `googleapis`/CrUX client, `finding_source` a one-value enum with `gsc` reserved for Phase 5. The Page Audit ask is refused on product grounds too: `apps/api` has zero outbound HTTP by construction and verification runs in the worker behind the rate limiter and circuit breaker, so an on-demand fetch bypasses both. **One finding survived and was the strongest in the review**: there was no pattern explorer, and the data for one had been sitting unreachable — `scorePatternImpact` and `compareByImpact` had zero callers, `GET /sites/:siteId/patterns` existed with no screen consuming it. ADR-0034's gap again. Delivered: `listPatternsRanked` (grouped and paged in SQL, LEFT JOINed so a pattern with no finding still appears — the load-bearing choice, since blocked/needs_review/unsampled patterns have no snapshot row and an inner join would drop exactly what a triage screen exists to surface), `listSnapshotsByPatterns`, `countPatterns`, a widened route, and `apps/web/app/sites/[siteId]/patterns/page.tsx` on the Stitch **Sitemap Analyzer** composition with its figures refused (no "Indexability 94%" — nothing measures index state; one `urls discovered` card rather than the Search Console submitted/discovered pair; `parsed at` rather than "Last Modified"). ADR-0038, DESIGN.md §7.5. **The decision worth carrying: the rollup is deliberately NOT `scorePatternImpact`.** That function re-derives severity from the table in force NOW, while a stored `audit_snapshot` carries the weight in force WHEN IT WAS PUBLISHED (ADR-0014) — re-scoring would silently restate history under new weights — and reaching it at all would mean rebuilding a `StratifiedEstimate` the row does not carry, which is ADR-0035's invent-the-missing-part defect. So `rollUpPatternImpact` repeats the arithmetic, shares the CLASSIFICATION (`isAbsenceOfEvidence`, a pure function with nothing to invent), and a test asserts the two agree wherever the stored weights match the current table. It is also **shaped as a measurement** — field names exactly `auditSnapshotSummary`'s — so the existing `<Estimate>` adapters render it and the ADR-0008 guard already polices it; a rollup with its own field names would have been a sampled figure the guard could not see. `rankScore`, the bare summed point value SQL orders by, never leaves the API and got the guard's **stricter second rule**: a name no module may read, **adapter included**, since an adapter that learned to format it would satisfy every other check in that file while rendering an interval-less estimate. **Four defects, three of them caught only by screenshotting the running app and none by any test** — fourth milestone running. A soft 404 answers HTTP 200, and the seeded data has one pattern `soft_not_found` at 200 beside another `ok` at 200; the cell coloured both healthy green. The first fix reached for `httpStatusTone`'s `isSoft404` flag and a test rejected it: that mapper scores a soft 404 `critical` while `severityTone` scores it `warning`, and **they disagree by design** — one answers "what did the wire say" (raw observations, no severity class), the other "how bad is this finding". A row carrying a class takes the mapper that owns it, and the disagreement is now asserted so nobody collapses it. Then: the affected column summed `ok` findings into "affected URLs", so a pattern whose 18 URLs all return 200 read `Affected 18` — on a mixed pattern it would have added healthy URLs to broken ones; fixed by splitting `measured` (not an absence of evidence) from `counted` (carries damage), kept separate rather than merged because an `ok`-only pattern is measured and fine, and collapsing them would report a working pattern as unmeasured. And the `patterns` KPI card used the FILTERED total, so filtering to "unsampled" showed `PATTERNS 0` beside a panel listing eight — the cards frame the run, the filter governs the table and its footer, which now names the filtered subset. The fourth was caught by the database: a `blocked` fixture with `observed_count: 30` was refused by `ck_audit_snapshot_evidence_tier_matches_coverage`, which requires a refusal to carry zero observed and zero estimate. **The rail now says "Analyses", not "Crawls"** (ADR-0039), reversing ADR-0027's label clause at the owner's direction after the external review reached the same objection the implementer had raised and had overruled. Only two user-visible strings used the word as product language; every heading behind the item already said "Runs" or "Sampling Analysis". `nav-rail.test.tsx` asserts it, scans every label for crawler words with the remedy in the message, and checks no item points at a route that does not exist. **Raised and not smoothed over: "Analyses" now sits directly above "Analytics"** — different screens, near-identical words; "Runs" was offered as the alternative and remains the fallback, and a test keeps the two items separate and correctly pointed. **The Stitch design is vendored** at `docs/design-source/` — 18 screens with their generated HTML, supplied by the owner. This retires the four PROVENANCE CAVEATs §7.1–§7.4 carried and the "Stitch is unreachable" dead path in §10, and it is a fix rather than a workaround: the source no longer depends on one machine's network. All 18 `tailwind.config` blocks are identical and match DESIGN.md §3 hex-for-hex, radii 2/4/8/12 and the six-size type scale exactly — so §2–§5 are now **verified rather than inferred**; what a config cannot settle is which element gets which value, and those layouts are still screenshot-matched and flagged as such. DESIGN.md's "13 desktop screens" was wrong (18) and it could not enumerate them; both fixed. One trap recorded: `technical_precision/DESIGN.md`'s PROSE describes a generic Zinc palette that contradicts its own frontmatter, the 18 configs and every screenshot — it is Stitch boilerplate, and the frontmatter is authoritative. Two isolation cases added, each with its own case per the D3e rule, each asserting the rival's rows exist BEFORE the isolation assertion per D3d, and each confirmed load-bearing by removing **that specific query's site predicate** per D3c; separately, removing the route's `assertSiteInOrg` makes the API answer 200 with the rival's template in the body, which is a different layer failing differently. Also found: a stale Turborepo cache replayed `web:test: no tests` in one full run while the suite had 130 — `--force` proved it; a cached vacuum reads exactly like a real one. Unchanged: no auth (ADR-0026), no migration, no schema change, and the twelve configured-but-unenforced limits (ADR-0030) are still unenforced and still reported. **Deferred with the direction decided**: Overview becomes the fleet population-and-impact funnel and hands its table to `/projects`, resolving the duplication D3i recorded as owed — Stitch's own overview is a per-project dashboard whose four headline figures are all refused or unmeasurable here, so the design does not settle it, and the funnel reuses this milestone's aggregation. Also surfaced, not decided: the review's evidence taxonomy (Google requirement vs recommendation vs heuristic) is the architecture plan's own open Phase 2 item — `finding_source` has one value today, so a five-way taxonomy would sort every row into one bucket.

