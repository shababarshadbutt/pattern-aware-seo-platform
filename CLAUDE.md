Pattern-Aware SEO Platform — Agent Guide

This file is read automatically by Claude Code (and should be treated as load-bearing by any other coding agent working in this repo). It tells you what this project is, what's already been decided, and what rules to follow so the codebase stays consistent instead of accumulating whatever style each session or contributor happens to reach for.

Read before making non-trivial changes
docs/architecture-review-and-action-plan.md — the why: the product direction, the phased build order (Phase 0 through 5), and a list of open decisions that haven't been made yet. Don't build ahead of the current phase without checking this. As of M6, this doc carries dated amendment sections through M0–M6, the post-M3 hardening pass, and both main promotions (PR #7, then the bundled M4+M5 promotion PR #9) — read to the end, not just the original text.
docs/CODING_STANDARDS.md — the how: full TypeScript/Node.js and PostgreSQL conventions. This file only summarizes the parts an agent is most likely to violate by default. As of M6 this includes a full §1.13 on composition correctness — the single most common real-bug category in this codebase (five confirmed instances across M2, M5, and M6: independently-correct, independently-tested pieces that break an invariant neither one owns once wired together) — plus recorded exceptions/additions on composite primary keys on partitioned tables, the Biome + narrow type-aware ESLint pass, a documented departure from "port the legacy algorithm" when the legacy algorithm is provably wrong, never using a sampling hash as a uniqueness key, capability-scoped config validation, and end-to-end-against-real-dependencies as a required check alongside per-package tests — check there before assuming the general rule applies unmodified.
docs/DESIGN.md — the UI spec: visual identity, tokens, component rules. Binding for anything touching apps/web.
docs/decisions.md — ADR log. Check here before re-litigating something already decided during implementation (tooling choices, version pins, schema exceptions, algorithm replacements, etc.) — as of M2 this includes at least ADR-0008, ADR-0010, and ADR-0012.

If a task touches something listed under "Open decisions" in the action plan (e.g. whether PopulationProfile duplicates existing tables, or the internal-vs-external auth question), stop and surface the decision instead of guessing.

Document precedence when two docs conflict

These docs were written at different times for different purposes, so a conflict between them is expected occasionally, not a sign something's broken. Resolve it in this order:

docs/decisions.md (ADRs) — the most recent, most specific, ground-truth record of what was actually implemented and why.
docs/DESIGN.md — wins over the architecture/tech-stack doc for anything about UI presentation, motion, or the frontend design-tooling recommendation. It's the more specific, later document for that surface. (Confirmed case: the architecture doc said skip Magic UI for the dashboard; DESIGN.md §8 names three specific motion moments where it's used. DESIGN.md wins — that's not a contradiction to "fix," it's the general recommendation being overridden by a more specific one for this product.)
docs/architecture-review-and-action-plan.md — the overall product/data-model/infra direction.
docs/CODING_STANDARDS.md — general code conventions; expect this one to accumulate documented exceptions as real tooling, schema, and algorithm decisions get made (e.g., a narrow type-aware lint rule sourced from a second tool alongside the primary linter/formatter, a composite primary key on partitioned tables, or a legacy algorithm replaced after a corpus test proved it wrong) rather than being rewritten each time.

When a conflict surfaces, add an ADR recording which side won and why — don't just silently pick one.

What this is

A pattern-aware, sampling-based SEO intelligence platform. It does not crawl every URL on a site. It collapses a sitemap's URLs into a much smaller set of patterns, samples each pattern statistically (with proper confidence intervals, not guesses), and verifies health with targeted HTTP checks — the entire point is auditing a 90M-URL site without making 90M requests. Any change that reintroduces "just crawl everything" thinking is working against the product's actual differentiator.

The core entity model (landed in M1) is organization → site → sitemap_run → sitemap_file → pattern → pattern_population / pattern_sample → sample_observation, plus audit_snapshot and sampling_health. site exists independently of any one audit run — this is the deliberate fix for the legacy tool's design, where the root entity was a one-off session with no memory of a site being audited again later. Trends, regressions, a fleet view across sites, and history to train a model on all depend on site being a durable entity, not an artifact of a single run. Don't reintroduce a run-rooted model even locally in new code.

As of M2, single-pass streaming ingestion is real and benchmarked (10,000,000 URLs, 681 MB peak RSS against a 1,536 MB budget, ~41,600 URLs/s, byte-identical resume). Pattern extraction does not faithfully port the legacy algorithm — the legacy algorithm had a real correctness defect (see ADR-0012, and the Non-negotiable rules below) and was replaced with a prefix trie after corpus testing proved the defect out. Don't assume "ported from legacy" describes packages/sitemap's pattern extraction; check docs/decisions.md first.

Repo layout
apps/
  web/       Next.js frontend (dashboard)
  api/       Fastify API
  worker/    BullMQ workers + Piscina pools
packages/
  database/   Schema shared by api + worker. Tenant-scoped queries are enforced at compile time via an
              opaque `Database` type with no ambient query methods and a single package entry point (`.`)
              — see `compile-guards.test.ts` and ADR references in `docs/decisions.md`. Don't add a
              second way to obtain a raw client that bypasses this. A narrow, test-only
              `packages/database/testing` export subpath (a migrated-pool factory for packages like
              `packages/pipeline` that need a real Postgres in their own suite) is the one sanctioned
              exception — decided at M6, implemented at M7 with both of its conditions met
              (ADR-0023): one compile guard asserts the exact sanctioned export set, and a second
              asserts the subpath is imported only from test files and never from `apps/*/src`, which
              is why `apps/api`'s tests live in `apps/api/test/` rather than beside the source. The
              `.` entry point is unchanged and no less restrictive — the harness hands back the same
              opaque `Database`, so a test can create and drop a database and still cannot write an
              unscoped query. `sample_observation` is unique on the URL, not on `url_hash` —
              see the Non-negotiable rules below before adding any new uniqueness constraint on a hash
              column. Isolation is enforced at two independent layers — compile-time (the opaque type)
              and schema-time (`UNIQUE (site_id, id)` plus FK pairing, closed post-M3) — each needs its
              own proof; don't treat one as covering the other.
  sampling/   Bounded min-heap-by-hash sampling (landed M2) plus Wilson score confidence intervals with
              finite-population correction and adaptive sample expansion (landed M3, validated against
              published reference values, not just the package's own arithmetic).
  sitemap/    Streaming SAX parser + pattern extraction (prefix trie — a deliberate, ADR-0012-recorded
              departure from the legacy algorithm, not a port of it). Maintains a large adversarial
              synthetic corpus test (10M-URL scale) alongside unit tests — see
              `docs/CODING_STANDARDS.md` §1.10 before touching this package with only small fixtures
              as evidence of correctness.
  http/       HTTP verification client (landed M5): HEAD-first, GET-only-on-suspicious, size-capped GET
              bodies, per-request rate limiting, circuit breaking. Its own test suite is deliberately
              entirely mocked at the transport boundary (see `docs/CODING_STANDARDS.md` §1.10 on why) —
              real-socket lifecycle bugs (e.g. the M6 `probe.ts` teardown defect below) only surface
              when this package is exercised end-to-end against a real server, not from its own suite.
  pipeline/   Orchestrates discover → ingest → verify → estimate → finalize (landed M6) behind per-site,
              per-tier queues (`{tier}:{siteId}:{stage}`). BullMQ appears nowhere inside this package —
              every stage takes its dependencies as injected parameters, so the package's own test suite
              runs against a real Postgres and a real local HTTP server with zero Redis involved; queueing
              is composed in from outside the package (see `apps/worker`).
  shared/     Shared types, utils, constants (zod-validated startup config, logger, domain error hierarchy).
              Config validation is capability-scoped (separate `dbConfig`/`redisConfig`/`httpConfig`
              sub-schemas an entry point composes from what it actually consumes), not one monolithic
              `getConfig()` — see `docs/CODING_STANDARDS.md` §1.7 and the M6 `runIngest` finding below.
  ml-client/  Client for the ml-service prediction endpoint
ml-service/   Separate Python service (model training + /predict) — see rule below
infrastructure/  Terraform/CDK for AWS
docs/         Architecture plan, coding standards, design spec, ADR log
Commands
bash
docker compose up -d        # Postgres 16 + Redis (ports overridable via POSTGRES_PORT/REDIS_PORT —
                             # see .env if a native Postgres/Redis install on the host already holds
                             # the default port)
pnpm install
pnpm dev                    # runs web + api + worker together (from repo root)
pnpm --filter <app> dev     # run just one app, e.g. pnpm --filter worker dev
pnpm build
pnpm lint
pnpm typecheck
pnpm test

Run pnpm dev from the repo root, not from inside an app folder — a package's own dev script only exists once that package has real source files.

Non-negotiable project rules

These come out of a specific architecture review and its critique (see the action plan doc for the full reasoning) — they are not arbitrary preferences, and deviating from them reintroduces problems that were already identified and fixed once.

No ORDER BY random() for sampling, ever, on tables that can grow large. Use the bounded min-heap-by-hash approach in packages/sampling — implemented and benchmarked at 10M-URL scale as of M2.
Confidence math must use the Wilson score interval with finite-population correction, not a naive normal approximation. This lives in packages/sampling — don't recompute it ad hoc elsewhere. (Check docs/architecture-review-and-action-plan.md's Phase 0 amendment and docs/decisions.md for the corrected FPC details verified during M0 — the original doc's threshold language here was found to need correction.) The degenerate-interval defect (a [0,0] interval falsely implying certainty from a partial sample) is now a database-level CHECK constraint, not just an application check, with an n = N exemption for a fully-counted population — see M1 notes below. The Wilson/FPC math landed in M3 (validated against published reference values, not just the package's own arithmetic); what M2 delivered before it was the sampling mechanism the math consumes — the min-heap — not the math.
HTTP verification is HEAD-first, GET only on suspicious results, and GET body fetches must be size-capped (soft-404 detection needs the first few KB, not the whole page). Cap how much of a pattern's sample is allowed to escalate to GET before flagging it for manual review instead of continuing. (Same note as above — the body-cap specifics were corrected during M0 verification; defer to docs/decisions.md.)
Never materialize a full URL population in memory or in a single query result. Everything works off streaming passes and pattern/file-level aggregates, not per-URL row sets at the 10M+ scale. M2's benchmark (681 MB peak RSS for 10M URLs) is direct evidence this is holding in practice, not just in intent.
A run that completes without throwing is not necessarily a run that succeeded. A sitemap parse that yields zero URLs from an accepted file is indistinguishable from a legitimately empty site unless it's flagged explicitly (an HTML error page parses as valid XML — this bit M2). Any code path that can "succeed" with an empty or suspicious result must signal that explicitly (structured warn log at minimum) rather than looking identical to an ordinary success. See docs/CODING_STANDARDS.md §1.5.
Multi-tenant isolation is a first-class concern, not an afterthought. Large per-URL/per-pattern tables are partitioned by site_id from schema creation (as of M1, six tables), not retrofitted later. Unscoped, cross-tenant queries against packages/database are a compile error, not a runtime risk, by construction (opaque Database type + single entry point) — see compile-guards.test.ts — and a schema-level cross-tenant FK gap found post-M3 is closed (UNIQUE (site_id, id) plus five repointed FKs). BullMQ queues and Redis keys are namespaced per site/tier ({tier}:{siteId}:{stage}) — done as of M6, closing the item pending since M2. Connection-pool budgeting is the one piece still not built. Fleet-wide auto-attach (a worker automatically picking up newly-added sites across organizations) needs a cross-organization read, which would punch a hole in this guarantee — deliberately left undecided as of M6; don't wire it in incidentally while touching queues or workers.
Query layer split: use Drizzle or Kysely (not Prisma) for anything in packages/sampling or packages/sitemap — this workload needs hash filtering, window functions, and CTEs close to raw SQL. Prisma is fine for simple CRUD (dashboard/admin, user/site management) if used there, but never for the sampling hot path. Drizzle Kit is the schema migration source of truth, but it cannot generate PARTITION BY DDL — partitioned-table migrations are hand-completed with three verification guards (banner comment, a relkind = 'p' test against a real database, a test asserting a PARTITIONED_TABLES constant matches what the database reports). Never run drizzle-kit push against this schema — see docs/CODING_STANDARDS.md §2.4.
Never make a uniqueness constraint out of a hash chosen for its sampling distribution properties. sample_observation was originally unique on the 32-bit FNV-1a url_hash, which — combined with the min-heap sampler specifically retaining the smallest hashes — clustered collisions in exactly the retained sample and silently dropped real observations. Fixed in M2 (migration 0001) by moving uniqueness to the URL. See docs/CODING_STANDARDS.md §2.2 before adding any new uniqueness constraint on a hash column.
"Port the legacy algorithm" does not mean reproduce a proven defect. If a large adversarial corpus test (not a small hand-written fixture) demonstrates the legacy algorithm is structurally wrong, replacing it is correct — provided the replacement is validated against the same corpus and recorded as an ADR. See docs/CODING_STANDARDS.md §1.12 and ADR-0012 (the M2 pattern-extraction rewrite). Don't treat this as license to casually diverge from legacy elsewhere — it applies specifically when a defect is demonstrated, not by default.
ML work lives in ml-service/ (Python), never inlined into the Node platform. The Node/TypeScript core stays as-is; ml-service/ reads features from the same Postgres database and serves predictions over a small internal endpoint or writes them back on a schedule.
Don't reach for: NestJS, Lambda for the main workers, MongoDB, SQS in front of BullMQ, or EKS — all considered and rejected for this workload in the architecture doc. Revisit only if a concrete gap shows up in practice, not speculatively.
Don't add a new PopulationProfile-style entity (or similar) without first checking whether pattern_shape_rules, structureClusters, or shapeStrata already cover it — this is still an open decision. M1's pattern_population/pattern_sample schema addresses the population/sample storage question but has not been confirmed as resolving the shape/stratum distinction specifically, and M2 did not touch this question either — don't assume it's settled without checking the architecture doc's open-decisions section.
Coding standards (see docs/CODING_STANDARDS.md for full detail)

The essentials an agent must not violate by default:

TypeScript strict mode everywhere; no any — use unknown and narrow it.
Named exports only; no default exports.
ESM only ("type": "module") — no require() in new code.
All async code uses async/await; no unhandled/floating promises (enforced via a type-aware lint rule — see docs/decisions.md for which tool covers this).
Structured logging (pino) — no stray console.log in application code, only in one-off scripts.
Every table, column, and index name is snake_case; every foreign key is <singular_table>_id; every table has created_at/updated_at. Partitioned tables use a composite primary key (site_id, id), not plain id — Postgres requires the partition key in any unique index or PK on a partitioned table (see ADR-0010).
All SQL is parameterized — no string-concatenated queries, ever.
Every exported function that isn't trivial gets a short TSDoc comment explaining why, not a restatement of the signature.
Environment variables are validated once at startup via a schema (zod), fail-fast, reporting all missing/invalid vars together rather than one at a time — never read scattered process.env.X calls through business logic.
Statistical invariants that must never silently hold a wrong value (e.g. the degenerate-interval case above) get a database CHECK constraint, not just an application-level assertion — "unwritable" beats "alerted on."
Tree/trie/streaming-aggregation code (packages/sitemap is the model) needs a large adversarial synthetic corpus test, not just small hand-written fixtures — three real bugs in M2 only surfaced at corpus scale.
Never put a uniqueness constraint on a hash column chosen for sampling/bucketing distribution, not collision resistance.
Workflow expectations
Check which Phase (0–5) a task belongs to in the action plan before starting; Phase 1 (sampling/population engine) work shouldn't get blocked on Phase 5 (SEO audit UI) polish, and vice versa.
When a change touches a decision the action plan marks open, say so explicitly rather than picking an answer silently.
Update docs/architecture-review-and-action-plan.md when a real architectural decision gets made or changed — it's meant to stay current, not freeze as a one-time snapshot. Prefer amending with dated findings over silently rewriting original text, so there's a record of what changed and why (this is what M0, M1, and M2 all did).
Record tooling/version/schema/algorithm decisions as ADRs in docs/decisions.md rather than only in a commit message or PR description — commit history isn't a substitute for a decision log someone can skim later.
Never commit .env, credentials, or AWS/SFTP keys. .env.example documents what's needed; real values stay local or in Secrets Manager.
New logic in packages/sampling (confidence intervals, sampling algorithms, statistical decisions) needs tests — this is the part of the system a wrong-but-plausible-looking answer would be most costly, and it's also the part hardest to eyeball-review. The same standard applies to anything asserting tenant isolation (e.g. compile-guards.test.ts) — these are correctness-critical, not incidental.
Before merging a schema migration, confirm it doesn't fight a native/local install shadowing a Docker service on the same default port (this bit M1 with Postgres on Windows) — use the POSTGRES_PORT/REDIS_PORT overrides rather than assuming the default port is actually reaching the container.
Before replacing a ported algorithm because a corpus test found a defect in the original, validate the replacement against the same corpus and write the ADR (see ADR-0012) — don't just swap it in silently.
If a required or requested review bot (e.g. GitHub Copilot code review) stops delivering reviews, diagnose before assuming misconfiguration: check branch protection/rulesets, confirm the bot's identity/ID is genuinely valid (it authored past reviews), confirm a REST/GraphQL reviewer request is accepted without error, and wait a reasonable interval (60–90s) for an async review to arrive. If the request is silently accepted every time but never fulfilled, and it worked then abruptly stopped, that's the signature of an exhausted quota/allowance, not a config bug — check the account's plan/allowance page (not API-reachable for personal-repo Copilot billing) before spending more time on repo-side config. When a bot review can't be obtained, a manual review pass is an accepted substitute, but record the substitution explicitly (commit message and/or the relevant test file) — a documented gap is fine, a silent one isn't.
Git conventions
Conventional Commits: feat:, fix:, chore:, docs:, refactor:, test:, perf:.
Branch names: feat/<short-description>, fix/<short-description>, or phase-<n>/<short-description> (or phase-<n>/m<milestone>-<short-description>) for work tied directly to an action-plan phase.
Don't push directly to main once more than one person is working in this repo — open a PR, even a small one.
Keep an unrelated large-diff change (a lockfile-rewriting package-manager upgrade, a dependency major-version bump) in its own commit/PR, separate from feature work — makes both easier to review and to revert independently.
Open PRs (don't just push branches) and confirm CI actually runs green in GitHub's environment, not just locally — a passing local run doesn't confirm the CI service containers (e.g. the Postgres service container M1 added) are configured correctly in the real environment. Resolved as of the bundled M4+M5 → main promotion (PR #9): git merge-base --is-ancestor run against main for every one of the seven feature/phase branches in the repo returns true for all seven — zero unmerged work anywhere — and main/develop came out content-identical (empty diff). Confirm a claimed merge/promotion state this way, directly, rather than from memory of which PRs were opened. Treat the PR/CI/branch-backlog concern raised after M1 and M2 as closed; the only remaining action is housekeeping — the seven now-stale branch refs still exist (their deletion was blocked by an auto-mode safety classifier) and need a person to run: git push origin --delete phase-0/m0-foundation phase-1/m1-schema-tenancy phase-2/m2-sitemap-ingestion phase-3/m3-sampling-core fix/review-findings-m0-m3 phase-2/m4-impact-scoring phase-2/m5-http-verification. Separate and still open: GitHub Copilot Code Review going silent since PR #4 (see the workflow-expectations bullet above) — a review-coverage gap, not a merge-verification one.
Milestone log
M0 (Phase 0 foundation) — complete. Tooling/enforcement (linting, type-aware promise rules, pre-commit hook, CI), real workspace packages, packages/shared (config, logging, error hierarchy), initial apps/web scaffold conforming to docs/DESIGN.md, docs/decisions.md established. See ADRs for tooling specifics and the architecture doc's Phase 0 amendment for the corrected FPC/body-cap details.
M1 (Phase 1 schema/tenancy foundation) — complete. Branched phase-1/m1-schema-tenancy off develop (includes M0); all four CI gates pass; 26 integration tests against a real Postgres 16. Delivered: the organization → site → sitemap_run → sitemap_file → pattern → pattern_population / pattern_sample → sample_observation model plus audit_snapshot and sampling_health, with site as a durable root entity (the deliberate fix over legacy's run-rooted session model); the degenerate-interval defect closed as a CHECK constraint with an n = N exemption; compile-time tenant isolation via an opaque Database type + single entry point + compile-guards.test.ts; the composite (site_id, id) primary key exception for six partitioned tables, recorded as ADR-0010. Two documented hazards: Drizzle Kit can't express PARTITION BY (hand-completed migration, three guards, never run drizzle-kit push); a native postgres.exe on port 5432 was shadowing the Docker container locally (fixed via overridable POSTGRES_PORT/REDIS_PORT, 5433 set locally).
M2 (Phase 1 streaming ingestion + pattern extraction) — complete. Branched phase-2/m2-sitemap-ingestion; all four CI gates pass locally; 57 tests across packages/sitemap and packages/sampling. Delivered and measured: a single streaming pass over a synthetic 10,000,000-URL corpus at 681 MB peak RSS (1,536 MB budget), 89 MB retained, 19 correct patterns, exact population counts, ~41,600 URLs/s, byte-identical resume. The bounded min-heap-by-hash sampler landed here (ahead of its original M3 slot) because the streaming pass needed it to draw a reproducible sample. Found and fixed a real defect in the legacy pattern-extraction algorithm (minimum-observation floor of 3, plus one path-position counter shared site-wide) that silently merged unrelated URL families into meaningless patterns while still reporting a fully successful run with exactly correct counts; replaced with a prefix trie, recorded as ADR-0012 — a deliberate departure from "port legacy," not a porting mistake, and flagged for a side-by-side validation against a real site before production cutover. Three further bugs (stranded literal siblings after a partial collapse, a bottom-up rebuild skipping its own just-merged subtree, fresh-node-as-leaf misclassification) were caught only by the adversarial corpus, not unit tests — now a standing testing requirement (docs/CODING_STANDARDS.md §1.10). Two parser bugs fixed: an oversize-file guard that never fired (the SAX parser processes a whole chunk synchronously) and an HTML error page parsing as valid, URL-less XML — indistinguishable from a legitimately empty site without an explicit signal (§1.5). Found and fixed a data-integrity bug shipped in M1: sample_observation's uniqueness constraint was on the 32-bit url_hash, and because the min-heap sampler retains the smallest hashes, the retained sample sat in the collision-dense low end of the hash space, silently rejecting real observations as duplicates; migration 0001 moves uniqueness to the URL (§2.2).
M3 (Phase 1 confidence math) — complete. Wilson score confidence intervals with finite-population correction, plus adaptive sample expansion wired to the min-heap's expansion bounds — packages/sampling. Validated against published Wilson reference values, not just the package's own arithmetic, which caught two wrong hand-computed expectations (a missed FPC shrink, a misquoted published value). Found and fixed: a float-epsilon defect at p̂ = 1 that would have violated audit_snapshot's CHECK constraint; confidence-band classification needed two threshold pairs, not one (ADR-0013), since width-relative-to-estimate and width-relative-to-population break in different regimes. Accepted trade-off: a mid-range-error large pattern is structurally capped at approximate confidence under a 400-probe first round. Tuning observation, not yet acted on: firstRoundSampleSize draws 30 of a 40-URL population — a small pattern stays statistically estimated when 10 more requests would make it a full census instead, for roughly the same request cost. Worth a future revisit of the first-round sizing heuristic.
Post-M3 hardening pass (PR #5) — complete, 2026-09-02. Nine Copilot Code Review findings across the M0–M3 stack, all confirmed real, fixed in one consolidated PR. The significant one: a cross-tenant FK gap in the M1 schema — the compile-time query guard was real, but a row could carry one site's site_id while a FK pointed at a different site's parent row, since only the FK column was constrained, not the (site_id, id) pair on the referenced side. Closed via UNIQUE (site_id, id) plus five repointed FKs; isolation.test.ts gained explicit cross-site cases. Also found: the GitHub Actions workflow never triggered on push to develop, only main/pull_request — every earlier "CI green" claim had only been checked on a PR's source branch, never after merge; .env.example's AUTH_SECRET="" failing its own validation and breaking the README's first documented command; several smaller correctness bugs (node-postgres returning bigint aggregates as strings, NULL-sort ordering, a memory-cap undercount, a false idempotency claim, a discarded XML root tag).
First main promotion — complete, 2026-09-02 (PR #7). main had held only the original scaffold until now; it carries M0–M3 plus the post-M3 hardening pass for the first time, with its own fresh CI run's job logs read directly (not assumed) to confirm the Postgres-backed tests actually executed rather than being skipped.
M4 (Phase 2 opening — Impact Score) — complete. severityFor ships with no default severity table — a missing argument throws rather than guessing. The severity table itself (410/404-first ranking) was ratified by the project owner, recorded as ADR-0014. Two rules locked in by tests: a refusal (HTTP 403) can never carry impact, enforced at the database level; total volume can outweigh severity class (50,000 redirect chains outrank 200 gone pages) — deliberate, catching the implementer's own initial backwards assumption. Copilot Code Review did not run on this PR (cause undiagnosed at the time) — a manual pass found one real, merge-blocking composition-shaped defect (the first of this shape in the project): the GSC-traffic-factor multiplier, documented as arriving "as one multiplication," could push a computed impact_score past what ck_audit_snapshot_impact_sane allows — neither the hook nor the constraint showed the defect in isolation, only their interaction. Bounded to (0, 1] before merge so the multiplier can only de-weight, never amplify.
M5 (Phase 2 — HTTP verification client) — complete, merged to develop at 338b959. 58 tests, entirely mocked at the transport boundary — deliberately, since a suite that actually probed real servers would reproduce the exact behavior the rate limiter and circuit breaker exist to prevent. Delivered: HEAD-first, GET-only-on-suspicious verification; the M4 escalation-cap policy wired into a real client, correctly counting an escalated check as two requests (HEAD + GET sniff); rate limiting charged per actual request, not per logical "check" (fixing a measured legacy defect: ~49.17 req/s sustained against a nominal 25 req/s ceiling). Delivered via a manual review pass substituting for Copilot Code Review, which stopped delivering reviews after four runs in a 90-second window on 2026-09-02 — diagnosed as an exhausted quota/allowance, not a config error (see the workflow-expectations bullet above). That pass found two real bugs and a second composition-shaped defect: the escalation cap only ever gated soft-404 sniffing, not the mandatory GET a host's HEAD-rejection forces (fixed with a new HEAD_NOT_SUPPORTED verdict, bounded to allowance + 1); plannedSampleSize: 0 silently disabled the cap via nothing_to_probe instead of flag_for_review (fixed by throwing InvalidSamplePlanError); composing a caller-supplied hook with the rate limiter silently dropped the hook, which is what charges the platform-wide request budget (now composes correctly). Host-strategy negotiation deliberately left undone, scoped as ADR-0016.
Bundled M4+M5 → main promotion — complete (PR #9). Confirmed via git merge-base --is-ancestor against all seven feature/phase branches (all true — zero unmerged work anywhere); main/develop content-identical after. See the Git-conventions bullet above for the pending branch-ref cleanup command.
M6 (pipeline orchestration) — complete, merged to develop at b9854b5. 320 tests, confirmed green in GitHub's own environment on both the PR and the develop push. packages/pipeline wires discover → ingest → verify → estimate → finalize behind per-site, per-tier queues ({tier}:{siteId}:{stage}), closing the queue/Redis-namespacing item pending since M2. BullMQ appears nowhere inside the package itself — every stage takes injected dependencies, so the package's own suite runs against a real Postgres and a real local HTTP server with zero Redis involved. Five defects, three found only by the end-to-end run against real dependencies (not by any per-package suite): probe.ts destroying undici response bodies raised uncaught AbortErrors arriving after the probe had already resolved; audit_snapshot.impact_score was bigint for a value that's fractional by construction; the escalation cap's comparison was inverted once wired into a real run, correct only in packages/http's own mocked suite. One found by CI but not locally: runIngest called the shared getConfig(), validating the entire environment when the stage needed neither Redis nor a database — passed locally only because the developer's own .env happened to have everything set; fixed by moving to capability-scoped config sub-schemas (docs/CODING_STANDARDS.md §1.7). One found by a manual pass and the one to pay the most attention to: upsertPatterns is additive by design (built for per-file flushes), but its caller instead accumulates in memory and writes once at the end while marking files parsed incrementally — a crash between those two steps means retry skips the already-marked files, silently and permanently undercounting the population while the run finishes "clean." Both guards now have regression tests, confirmed load-bearing by deliberately neutralizing each. Separately: turbo.json declared no env for the test task, so Turborepo silently filtered CI's real DATABASE_URL out — the DB integration tests had only ever passed because a fallback default happened to coincide with the service container's connection string; declared now. Two decisions deliberately deferred rather than guessed at: fleet-wide auto-attach (needs a cross-tenant read — see the multi-tenant-isolation rule above) left unwired; packages/database's single-entry-point export widened only via the new packages/database/testing subpath decided here (see the Repo layout section above), not by loosening . itself. ADRs 0018–0022 recorded. This is the pipeline orchestration milestone the M5 entry referenced as "not yet named" — it's landed.
M7 (API + evidence screens) — complete, 2026-09-03. 356 tests. First milestone begun by another session and finished by review: ~1,900 lines arrived uncommitted, were committed verbatim as a checkpoint so their diff stayed separable, then repaired. Kept because it was good: <Estimate> enforces ADR-0008 via a discriminated union (an interval-less estimate is unconstructable, not merely discouraged), docs/DESIGN.md compliance is structural (Tailwind's colour and radius namespaces reset to nothing, so rounded-2xl does not compile), and the seed script runs the real estimator rather than typing numbers in. Seven defects, all found by probing the running system: the important one was routes/patterns.ts building a SiteScope and never calling findSiteById, so both pattern routes read another organization's rows on a caller-supplied siteId — the check siteScopeWithin's own doc says the caller owes, latent only because one organization exists; the regression test seeds a second tenant with real rows and was confirmed load-bearing by removing the guard. Also: no runtime validation anywhere, so a bad UUID returned 500 with the raw SQL in the response body (now zod via a type provider, ADR-0025); authenticatedOrganizationScope stamping origin: "request" with no session in existence, attributing a hardcoded identity to a user in every audit line (now systemOrganizationScope, ADR-0026); OrganizationNotSeededError promising a 503 that nothing implemented, so it returned a generic 500 — §1.9's own shape, a comment asserting a guarantee the code does not make; a site with no finished run returning 404, indistinguishable from a typo in the URL; and impact_score rendered bare with no ~ and no interval despite being point_estimate x severity_weight and therefore exactly as estimated as the count it weights (ADR-0024 derives the bounds — severity_weight and confidence_level were written to audit_snapshot and never selected, so no migration was needed). Zero tests existed for any of it and --passWithNoTests kept CI green over that; 35 now cover the API and screens, two of which found real defects rather than confirming behaviour: patternStatusTone called a blocked host critical, contradicting both severityTone and the enums.ts warning that a host refusing us is not a site defect, and the confidence band reached the reader through hue alone. ADR-0008's own enforcement clause ("a CI test asserts no page formats a sampled figure outside it") had never been implemented — it exists now, naming file, line and remedy, verified by reintroducing the exact violation. Composition again, third milestone running (§1.13): buildApp took the whole validated Config, so building it in a test validated REDIS_URL for routes that never touch Redis — the same defect as M6's runIngest, recurring in a different package within a day of being written down. packages/database/testing landed as §1.4 recorded it, with both attached conditions enforced by tests rather than review (ADR-0023); the first version of the import guard flagged a docblock that merely described the rule, so a grep-shaped guard needs verifying both ways. pipeline's duplicated harness deleted. Four smaller things: seed:demo and db:migrate never loaded .env, so the seed script told you to create a file it then ignored; the demo run was stamped isDryRun: false and status complete, making manufactured evidence indistinguishable at the data layer from a measured audit; WEB_API_URL was read with a fallback and declared nowhere (§4's shape, where Turborepo filters it out and setting it appears to do nothing); and the legacy Sitemap_Migration backend holds port 3001 locally, the same collision class as the native Postgres on 5432 that bit M1. Auth deferred deliberately, and DESIGN.md's slide-over, Cmd+K palette, j/k navigation, density toggle, sparklines and Magic UI moments scoped to D3 and named there rather than quietly dropped. ADRs 0023–0026 recorded. docs/DESIGN.md moved into docs/ to match where CLAUDE.md and six other references had always said it was.
