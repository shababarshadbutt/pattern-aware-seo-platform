// Schema, Drizzle client, and the SiteScope-gated repository layer land in M1.
// Nothing here yet on purpose: the tenancy boundary and partitioning strategy
// are decided in that milestone and adding tables ahead of it would be exactly
// the retrofit the plan exists to avoid.
export {};
