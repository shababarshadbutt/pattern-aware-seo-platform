// Bounded min-heap-by-hash sampling, Wilson score intervals with
// finite-population correction, and adaptive expansion land in M3.
//
// Nothing here yet on purpose. This package is the one part of the system where
// a wrong-but-plausible answer is most expensive and least likely to be caught
// by review, so its tests are written before its implementation.
export {};
